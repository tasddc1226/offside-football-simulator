# 훈련 선택 원인 분리 — 81cb696

상태: `EXPERIMENT_NOT_REFERENCE_POPULATION`. 밸런스 인수·참조집단 발행 자료가 아니다.
2026-09-05, Node 22.23.1에서 실행했다. 상시 CI에는 이 배치를 추가하지 않는다.

## 실행 조건 및 원본

- source commit: `81cb69649a7b909c31b94e0faa7f2761a9bf832c`
- 실행 전후 domain/content/tooling tracked diff는 비어 있으며 provenance가 일치했다.
- ruleset / Legacy `1.1.0`, content pack `0.3.0`
- 포지션별 seed index `0..7`, 요청 20시즌, ROLE/TECHNICAL 각 32회, 총 64회
- 64개 모두 20시즌을 실현했다. 조기 은퇴 경력은 이 작은 표본에서 관측되지 않았다.
- 계약·역할·사건 선택 정책은 public opportunity로 고정하고 훈련만 바꾼다.
  두 focus를 모든 연령에 적용하므로 30세부터 MENTAL로 전환하는 기본 opportunity와는 다르다.
- 공개 선택 이후 경로가 갈라질 수 있다. 같은 seed라도 이후 RNG 소비가 같다는 주장은 아니다.
- 기본 생성기 회귀: 보관된 random GK seed 1 행 전체와 archive/result hash 일치.
- [전체 원본 JSON(gzip)](diagnostics.json.gz)은 시즌별 OVR, 성장 원인, 출전, 시즌말 선발 요약,
  포지션 통계/per90, 원시 5축, archive/result hash를 포함한다. 개인정보·실서버 커리어는 없다.
- 압축 해제 원본: 11,001,941 bytes,
  SHA-256 `48ba9990b1181bae0e67aebf7f60d4dcc42cd7707419673ad171f3d7ca9cd069`.
  파일의 source에 생성기·진단기·ruleset·pack·Legacy policy checksum을 보관한다.
  선언만 된 bundle hash는 null이며 동결 번들 검증으로 표현하지 않는다.

```sh
pnpm exec tsx tooling/scripts/legacy-training-diagnostics.ts \
  --count 8 --seasons 20 --out artifacts/legacy-training-diagnostics.json
```

출력 파일이 이미 있으면 실패한다. `--count` 상한은 8, 최대 64회이며 공식 population 출력과
형식이 다르다. 공식 40,000명 모집단에 병합하거나 이 결과로 기존 결과를 재채점하지 않는다.

## 비교

아래 출전율은 커리어별 `총 minutes / 총 possibleMinutes` 비율의 포지션 내 평균이다.
ROLE → TECHNICAL 순이며 포지션당 8쌍이다.

| 포지션 | 평균 최종 점수 | 최고 점수 | 평균 peak OVR | 평균 출전율 |
| --- | ---: | ---: | ---: | ---: |
| GK | 62.00 → 62.00 | 70 → 70 | 68.00 → 68.00 | 23.47% → 23.47% |
| DF | 73.63 → 74.00 | 86 → 86 | 67.38 → 67.50 | 45.10% → 46.04% |
| MF | 57.25 → 57.25 | 72 → 72 | 68.25 → 68.25 | 23.53% → 23.53% |
| FW | 62.13 → 63.38 | 78 → 78 | 68.50 → 68.88 | 28.32% → 29.78% |

32쌍 중 출전 총계·최종 점수가 달라진 것은 두 쌍이다.

- DF seed 4: 79 → 82점, TECHNICAL 출전 +2,660분. 5축은
  `95/64/66/83/80 → 98/66/76/82/80`.
- FW seed 6: 61 → 71점, 출전 +2,945분. 5축은
  `59/64/39/85/60 → 73/77/52/89/60`.
- 5축 순서는 성취/기여/장기성/관계/서사다. MF seed 7은 기여 축 62 → 63이나
  반올림된 최종 총점은 65로 같다. 같은 총점이 내부 성장·통계까지 같다는 뜻은 아니다.

결산 훈련은 이미 끝난 시즌 경기에 소급되지 않는다. 다음 시즌에는 작은 능력치 차이가
OVR·가중 선발 점수의 정수화와 경쟁 경계를 넘지 못할 수 있다. 선발은 시즌 전체 고정값이
아니라 매 경기 form/fitness/morale/관계/출전 가능 상태 등을 사용하여 재계산한다.
원본 `selection`은 시즌말 요약이므로 전체 경기의 순위 이력이라고 해석하지 않는다.

차이가 나타난 최초 출전 시즌을 보면, DF seed 4의 시즌 18은 두 경로 모두 OVR `62→62`지만
출전 `395/1800분 → 1015/1890분`, 시즌말 순위 `3 → 2`였다. FW seed 6은 시즌 2에서
OVR `60→62 / 60→63`으로 갈라지고 시즌 7에 출전 `70/1260분 → 800/1260분`,
시즌말 순위 `3 → 1` 차이가 나타났다. DF 시즌 18의 태클/가로채기/공중볼 per90 합은
약 `7.06 → 6.74`로 출전 증가와 경기당 기여가 반드시 같은 방향은 아니다.
FW 시즌 7의 득점+도움 per90은 `0 → 0.5625`이며 ROLE은 70분의 작은 분모다.
속성·조건·선택의 누적 경로가 달라질 수 있으므로 이 시즌말 표만으로 특정 선발 항목 하나의
효과나 이후 전체 경기의 동일 RNG를 주장하지 않는다.

## 판단

훈련 선택의 출전 효과가 전혀 없다는 가설은 이 표본에서 성립하지 않는다. 그러나 ROLE의
집중 분배·성장률을 바꾸거나 TECHNICAL을 최적 전략으로 확정할 근거도 충분하지 않다.
모든 표본은 20시즌 요청이라 공식 1~20시즌 층화 분포 목표와 비교할 수 없다.
LEGEND 목표 달성·포지션 공정성·재미 검증도 대체하지 않는다.

다음은 두 차이 경로의 성장/경쟁 경계와 낮은 출전 경력의 원인을 검토하고, 새 1.1 후보에서
필요한 최소 변화만 선택한다. 총점 가산, 분위수 역보정, 구버전 1.0 재생 변경은 하지 않는다.
