# T-4-018: SCR-017 이적시장 화면 eyebrow를 시장 사유별로 (PR #78 시각 검증 후속)

## 목표

> **D-61(2026-09-05 16:02 사용자 지시)**: 새 테스트 코드를 작성하지 않는다. 기존 테스트가 문구 변경으로 깨지면 기대값만 갱신한다.

T-4-014(PR #78) 360px 스크린샷에서 발견: `career.$careerId.offers.tsx`의 비-FIRST_CONTRACT `ScreenIntro`가 시장 사유와 무관하게 eyebrow `"계약 만료·FA"`를 고정으로 쓴다. INTEREST(타 구단 관심)·LOAN_END(임대 종료)·PRE_NEGOTIATION(재계약 사전 협상) 시장에서도 "계약 만료·FA"로 보여 바로 아래 "현재 계약 · 시장 이유: 타 구단 관심"과 모순된다. eyebrow를 시장 사유별 라벨로 바꾼다.

## 참조·선행

- 정본: [화면 02](../../screens/02-career-hub.md) SCR-017, [T-3-005](T-3-005.md) §1(시장 사유 라벨), [T-4-014](T-4-014.md).
- 선행: **T-4-014(PR #78) 머지 뒤** 최신 `origin/main`에서 시작한다(같은 파일 `offers.tsx` 소유권 충돌 방지).
- 코드: `apps/web/src/routes/career.$careerId.offers.tsx` `ScreenIntro eyebrow="계약 만료·FA"`(약 310행), `apps/web/src/shared/transfer-view.ts` `MARKET_REASON_LABEL_KO`.

## 1. 작업

- 비-FIRST_CONTRACT 분기의 eyebrow를 `pending.market.reason`으로 정한다: `EXPIRED` → `"계약 만료·FA"`(기존 유지), 그 외 → `MARKET_REASON_LABEL_KO[reason]`(`타 구단 관심`·`임대 종료`·`재계약 사전 협상`). title `"이적시장 제안 비교"`·description은 그대로 둔다(e2e 단언 보호).
- FIRST_CONTRACT 분기(`"새로운 유니폼"`)는 건드리지 않는다.

## 2. 인수 조건

- 전체 체인: `pnpm install --frozen-lockfile && pnpm lint && pnpm lint:deps && pnpm typecheck && pnpm content:validate && pnpm test && pnpm build && pnpm --filter @offside/web check:bundle && pnpm --filter @offside/ui check:contrast && E2E_PORT=5227 pnpm --filter @offside/web e2e`.
- PR 본문: 사유별 eyebrow 표(FIRST_CONTRACT/EXPIRED/INTEREST/LOAN_END/PRE_NEGOTIATION).

## 범위·작업 규칙

- 변경 허용: `apps/web/src/routes/career.$careerId.offers.tsx`(+깨진 기존 test 기대값 갱신만). **금지**: 그 외 전부.
- 서브에이전트·병렬 fork·`/simplify` 금지. `docs/tracking/worker-brief-template.md` 제약을 따른다.
- 브랜치 `T-4-018-offers-eyebrow`, 커밋 `T-4-018: ...`. `gh pr create`가 막히면 push + `PR_BODY.md` + `DONE <sha>`로 멈춘다.
