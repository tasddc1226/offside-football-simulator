# Phase 5 밸런스 재설계 실행 계획

상태: 실험·설계 중 (2026-09-05). 사용자가 밸런스 후속 분리 대신 이번 Phase 5에 포함하기로 결정했다.
PR #77은 검증이 끝나기 전까지 Draft로 유지하며 main에 병합하지 않는다.

## 재개 현황 — 2026-09-05

PR #96으로 Mac self-hosted runner가 main `93cfcf2`에 반영됐고 실제 main CI와 staging 배포가
성공했다. 계정 결제 상태를 변경한 것은 아니지만 실행 차단은 해소됐다. #91/#92/#95의 최신
통합 head는 새 runner에서 검증 중이며, Phase 5는 여전히 별도 인수 후에만 병합한다.

1.1 후보 표본은 `phase5-population-5-policy-isolation`로 구분한다. population ID에 선택 정책과
전체 provenance의 canonical SHA-256을 포함해 random/opportunity/mixed, 생성기·checksum·표본 수가
다른 실행이 같은 ID를 쓰지 않도록 한다. 기존 1.0 ruleset/v3 프로토콜의 ID는 보존한다.
이 변경은 식별 충돌 방지이지 선택 정책의 채택이나 분포 인수가 아니다. v5는 아직 등록·발행 대상이
아니며 기존 publisher/runtime validator의 등록 프로토콜 제한을 풀지 않는다.

## 이전 통합 차단 기록 — 2026-09-05

GitHub Actions 실행 `33963258035`(PR #91)와 `33963265827`(PR #92)는 코드 실행 전에
계정 결제 실패 또는 지출 한도 오류로 runner가 시작되지 않았다. 검사 단계가 없는 외부 차단이며,
코드 검사 성공이나 코드 회귀로 해석하지 않는다. 유료 설정 변경·검사 우회 병합·직접 배포는 하지 않는다.
현재는 self-hosted workflow가 포함된 새 head로 재실행한다. 과거 ubuntu workflow의 단순 재시도는 하지 않는다.

재개 순서는 #91(이슈 #86–89) → #92(base를 main으로 변경, #56/#62) → 확장 QA staging →
Phase 5다. #61은 기존 임대 수락·완주·복귀 경로의 로컬 UI/도메인 검증 근거로 닫았다.
Phase 5의 ruleset/Legacy 1.1.0은 여전히 후보이며, 참조집단 발행·기본값 전환·완료 판정은 하지 않았다.
외부 세션의 #94는 구버전 `season_settled` 이벤트 유실 가능성을 PR 댓글로 전달했고 파일은 수정하지 않았다.

재개 작업 중 로컬 타입 검사 9개 작업, 웹 437건, engine-client 102건, content 406건,
domain 제안/시장/선발 55건과 수정 패키지 lint·의존성 경계 검사가 통과했다. 이는 명시한 로컬
검증 범위이며 최종 head의 전체 API/E2E·원격 CI·스테이징 검증을 대체하지 않는다.

## 2026-09-05 재개: 최신 main 및 제안 정보 정합성

사용자가 Phase 3·4 잔여 이슈와 확장 staging 검증 이후 Phase 5까지 재개하도록 지시했다.
main `050cf86`을 통합한 `3d19c9c`에는 시장 중복 개방 방지, 감독 RNG 분리, 실시간 부상·대표팀
조건 수정이 포함되므로 과거 v2 표본을 새 head의 분포로 재사용하지 않는다.

같은 seed 0..39 / 포지션별 40개 / 요청 1..20시즌 / Legacy 후보 1.1.0을 새 번들로 재실행했다.
160개 명령 재생·snapshot 검증은 통과했지만 ICON/LEGEND와 80점 이상은 모두 0건이었다.

| 포지션 | 평균 점수 | 최고 | 평균 출전 비율 | 평균 peak OVR |
| --- | ---: | ---: | ---: | ---: |
| GK | 33.9 | 66 | 11.8% | 65.9 |
| DF | 47.1 | 70 | 32.2% | 65.4 |
| MF | 36.7 | 74 | 17.0% | 65.8 |
| FW | 37.2 | 73 | 15.6% | 66.0 |

무관은 8건이며 80점 이상 0건이다. 20시즌 요청 표본은 8건뿐이므로 장기 경력 분포로 일반화하지 않는다.
번들 SHA-256: `1af40acd1c10dfeae0626d6fc96ec2680ec2f202e4328f61b13d6408183fe831`.
이 실행은 새 규칙이 아닌 ruleset 1.0.0의 점수 후보 파일럿이다.

독립 코드 감사에서 [#93](https://github.com/tasddc1226/offside-football-simulator/issues/93)을 확인했다.
제안의 전술 적합도가 실제 선발 계산과 무관한 난수이며 경쟁 순위는 OVR만 비교하고 있었다.
이를 고치기 전에 공개 수치를 읽는 계약 전략을 최적 플레이 증거로 취급하지 않는다.

### 새 규칙 후보의 최소 범위

- ruleset 1.0.0 JSON·기존 golden과 기존 Legacy 1.0.0 결과는 보존한다.
- ruleset 1.1.0 후보에서 팀·시즌으로 고정한 별도 로스터 스트림을 미리보기와 시즌 시작이 공유한다.
  선수 이름·UUID·능력치에 따라 경쟁자의 실력이 움직이지 않는 공통 세계 로스터다.
- 전술 적합도 및 경쟁 순위는 실제 선발 계산을 이용한다. 계약상 역할 약속은 별도로 유지하며,
  미리보기 이후 선수·감독 조건이 달라지면 예상치도 달라질 수 있다.
- 엔진의 실행·복구 및 화면 조회는 저장 커리어의 rulesetVersion을 사용한다. 새 기본값을
  기존 커리어에 자동 대입하지 않는다. 신규 버전은 명시적인 DEV/expanded QA에서만 선택한다.
- 콘텐츠 사건은 바꾸지 않고 0.3.0의 호환 룰셋 목록만 확장한다. events/chapters 및 그 checksum은 보존한다.
- 이 후보에서 성장 계수·선발 가중치·관계 이월 수치의 일괄 인상은 아직 하지 않는다.
  정확한 정보로 선택한 기회가 실제 출전·성장으로 연결되는지 먼저 재생한다.

역할 재협상 성공 뒤 경쟁 예상 순위가 남는 누락도 수정했다. 1.1 후보의 `NEGOTIATE ROLE` 성공 시
기존 순수 projection으로 제안의 전술 적합도·경쟁 예상 순위를 다시 계산한다. 최초 계약은 해당 경로의
감독 신뢰 특례를 보존하고, 1.0에서는 이 계산을 호출하지 않는다. 민감도를 명시한 단일 고정 fixture로
기존 코드의 rank 3/기대 2 실패와 수정 후 협상→수락→START_SEASON 일치를 확인했다. 협상 RNG는
기존과 같이 한 번만 소비한다. 자연 생성 제안 경로와 1.0 golden을 포함한 6개 파일/42개 검사가 통과했다.
이는 후보 코드의 로컬 회귀 근거다. 최종 통합 CI·후보 환경 화면 검증 전에는 #93 전체를 닫지 않는다.

### 선택 정책 진단

기존 무작위 정책을 유지하며 `--strategy opportunity|mixed`를 별도 진단 옵션으로 추가한다.
opportunity는 공개된 경쟁 예상 순위·적합도를 비교하고, 순위가 같고 적합도 차이가 10 미만이면
잔류를 우선한다. 같은 포지션군의 감독 역할 제안은 수락하고 30세 전에는 역할 훈련, 이후 정신 훈련을 선택한다.
이벤트 결과·잠재력·미래 RNG는 읽지 않는다. mixed는 커리어 시작부터 고정한 해시로
무작위/기회 우선 정책을 절반 확률로 나눈 진단층이다. 어느 것도 실제 사용자 분포라는 주장은 아니다.
최종 참조집단 정책과 허용 범위는 대규모 배치 전에 별도로 고정하며, 진단 결과를 그대로 발행하지 않는다.

이름의 `opportunity`는 최적 전략을 뜻하지 않는다. 현재 `growth.ts`에서 ROLE 훈련은 모든 훈련에
공통인 역할 가중 분배를 사용하며, PHYSICAL/TECHNICAL/MENTAL처럼 별도 집중 그룹에 추가 배분하는
선택은 아니다(D-39의 기존 설계). 따라서 계약·역할 수락·훈련을 함께 바꾸는 이 정책의 점수 차이를
계약 선택 하나의 인과 효과나 플레이 상한으로 해석하지 않는다. 잠재력 상한과 배분 순서도 있으므로
다른 훈련이 모든 경력에서 우월하다고 단정하지 않는다.

다음 원인 분리는 기존 계약/역할/이벤트 정책을 고정하고 훈련 선택만 비교하거나, 훈련을 고정하고
계약 선택만 비교하는 방식으로 수행한다. 같은 seed 집합을 사용하되 선택 이후 RNG 소비 경로가
달라질 수 있음을 보고한다. 출전→성장→선발의 피드백, 포지션별 실제 per-90 성과와 평가 기준,
관계·서사 축의 누적 근거를 함께 기록한다. 이는 아직 실행 결과가 아닌 진단 설계다.

### 1.1 제안 예측 후보: 800명씩 비교한 진단

ruleset/Legacy 1.1.0, pack 0.3.0, 포지션별 seed 0..199, 요청 시즌 1..20을 각 10회
순환했다. random과 opportunity는 같은 seed 집합이지만 여러 선택 정책이 함께 달라지므로
계약 선택만의 인과 효과라고 해석하지 않는다.

| 정책 | 평균 점수 | 최고 | 75점 이상 | 80점 이상 | 90점 이상 | 평균 출전 비율 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| random | 39.57 | 76 | 2/800 | 0/800 | 0/800 | 20.3% |
| opportunity | 45.61 | 88 | 36/800 | 16/800 | 0/800 | 30.1% |

20시즌 요청은 정책별 40명뿐이다. opportunity의 포지션별 평균은 GK 45.60, DF 50.47,
MF 40.22, FW 46.16이다. 공개 제안 정보와 역할 수락을 활용한 정책에서 출전·관계·기여가
개선되는 신호는 있지만 목표 분포와 포지션 공정성을 통과한 것은 아니다. 40,000명 발행을 시작하지 않았다.

**산출물 무결성 주의:** 실행 뒤 `content:validate`에서 미배포 1.1.0 manifest의 선언 checksum이
표준 CLI 계산과 다름을 발견했다. 아래 실행은 잘못 선언된 checksum을 provenance에 포함하므로
공식 참조집단이나 인수 증거로 사용하지 않는다. 수정 후 새 번들·새 디렉터리에서 재실행해야 한다.
raw 행을 고쳐 checksum만 교체하거나 기존 checkpoint를 이어 쓰지 않는다.

manifest를 표준 계산값 `6daaac9d2d4d92b665ca8fa3e2d7ed2179174cb2104df63c0f9988cdc5669b68`로
바로잡고 `content:validate`를 다시 통과했다. 실제 1.1 시장 제안의 수락 전 예상치와
수락 후 START_SEASON의 전술 적합도·경쟁 순위를 비교하는 회귀 검사도 통과했다.

- 동결 번들: `429caf53260b74c0f8ab7d54dbd00667face999b3deb5ae00188cc05598fab6b`.
- random 결과 SHA-256: `726b42b9444c23cfd099f98798f7879bdb7d6218884a56104dc751d7027a17be`.
- opportunity 결과 SHA-256: `4cc2ecdb14b794174e5171c46a39b7b094adde99df5d9c07ef7910b8c8069632`.
- 각 정책의 normal/accelerated 80행 parity는 일치했다. 이는 실행 경로 동등성이지 manifest 검증이 아니다.
- 정책별 결과의 population ID도 아직 같으므로 동시 발행 전 ID/프로토콜 구분을 확정해야 한다.

## 변경 전 기준선

`phase5-population-2-registered-choices`의 4만 커리어 실행은 계속 보존한다.
등록된 콘텐츠와 실제 명령을 사용하는 유효한 **변경 전 기준선**이지, 새 밸런스의 합격 증거가 아니다.
실행 중인 번들·체크포인트를 바꾸거나, 유리한 seed만 추려 참조집단으로 사용하지 않는다.

실행은 54분 21초에 완료됐다. 전체 ICON 2건(0.005%), LEGEND 0건, REMEMBERED 7,687건(19.22%).
다만 후속 UI 검토에서 KEEP 거절 편향을 발견하여 공식 참조집단으로는 사용하지 않는다.
원본은 [별도 증거 manifest](evidence/phase5-baseline-v2/manifest.json)와 압축 파일로 보존한다.

- MF:1279: 20시즌, 11회 우승, 24,275분, Legacy 73 (92/76/100/24/53).
- DF:699: 20시즌, 13회 우승, 11,455분, Legacy 71 (100/58/100/26/51).
- 이적 때 감독 신뢰 40, 주장 관계 0, 팬 관계 30% 이월이 반복된다. 임대 복귀도 관계를 재설정한다.
- 주장 관계와 주장 직책은 다른 값이다. 관계 값 0을 곧바로 인성 실패나 리더십 부재로 해석하지 않는다.
- 낮은 출전은 성장 경험과 다음 선발 경쟁에 영향을 준다. 반면 장기성은 출전이 적어도 20시즌에 100에 도달한다.
- 기존 승격 태그와 Legacy의 실제 승격 원장이 서로 다른 조건을 사용한다. 두 의미를 같다고 표시하지 않는다.

## 실행 순서와 채택 기준

1. **원인 분리:** 같은 seed에서 계약 정책만 바꾼 paired replay로 이적·관계·출전·성장 차이를 기록한다.
   RNG/능력치/이벤트 성공을 강제로 바꾸지 않는다. 작은 진단 표본을 전체 분포라고 부르지 않는다.
2. **후보 평가식:** 현재 관계 수치와 커리어에 남긴 관계 성과를 구분하고, 실제 출전 노출과 전성기·지속성을
   함께 평가하는 후보를 실험한다. 단순 총점 가산, OVR 직접 가산, 분위수에 맞춘 역보정은 사용하지 않는다.
   서사는 기록된 사건·태그만, 장기성은 활동 근거를 평가한다. 우승 없는 고기여 경로도 유지한다.
3. **게임 경로:** 출전·성장·관계의 구조적 문제가 확인된 경우에만 새 룰셋 또는 명시적 버전 경계를 설계한다.
   Phase 3·4의 기존 로직을 무조건 바꾸거나, 기존 ruleset `1.0.0`의 재생 결과를 조용히 변경하지 않는다.
4. **버전 호환:** Legacy `1.0.0` 원본·정의·hash는 유지한다. 새 계산 정책은 별도 버전으로 관리하고,
   보관·가져오기·동기화에서 계산 버전과 참조집단을 함께 고정한다. 기존 결과의 자동 재채점은 하지 않는다.
5. **파일럿 → 전체 재생성:** 사전 고정한 seed/플레이 정책으로 작은 표본과 경계 사례를 검증한 뒤,
   확정된 생성기·정책 checksum을 고정하여 포지션별 10,000개를 다시 생성한다.
   원시 5축 점수·출전·우승·관계 진단도 남겨 총점만 보고 판단하는 일을 방지한다.
6. **인수:** 기능 회귀, 구버전 결과 보존, 포지션 공정성, 무관 경로, 실제 분포, UI/API/CI를 함께 통과한 뒤
   문서·PR 결과표를 갱신하고 main 병합 및 staging 배포 상태를 확인한다.

## 수치 목표와 주의점

기존 설계 목표(LEGEND 약 2%, ICON 약 8%, REMEMBERED 약 30%, 80점 이상 약 10%, 무관 중
80점 이상 약 3%)는 아직 충족하지 못했다. 표본 크기나 checksum 검증만으로 이 목표를 통과 처리하지 않는다.
`약`의 운영 허용 범위와 모집단 분모는 아래와 같이 새 전체 배치 전에 고정한다.
목표를 만족시키려고 표본을 사후 선별하거나 목표를 몰래 낮추지 않는다.

### 분포 판정 기준 고정 — 2026-09-05

이는 기존 목표를 바꾸는 점수 조정이 아니라, 모호했던 `약`의 판정 범위를 명시하는 결정이다.
현재 후보가 통과했다는 뜻은 아니다. 새 유효 파일럿과 최종 전체 배치를 실행하기 전에 기록한다.

| 지표 | 분모 | 목표 | 운영 허용 범위 |
| --- | --- | ---: | ---: |
| BAND-LEGEND | 전체 40,000명 | 2% | 1.5~2.5% |
| BAND-ICON | 전체 40,000명 | 8% | 6~10% |
| BAND-REMEMBERED | 전체 40,000명 | 30% | 25~35% |
| totalScore ≥ 80 | 전체 40,000명 | 10% | 8~12% |
| 무관 중 totalScore ≥ 80 | 최종 원장상 팀 트로피 합계가 0인 커리어 | 3% | 2~4% |

- 각 포지션군 10,000명, seed index 0..9999, 요청 길이 `1 + (index mod 20)`을 유지한다.
  강제 은퇴로 짧아진 행도 제외하지 않는다. 실제 20시즌 경력만 추려 전체 분모를 대체하지 않는다.
- 밴드는 서로 배타적이며 80점 이상은 ICON 일부와 LEGEND를 합한 별도 지표다. 둘을 합산하지 않는다.
- 무관은 `END-UNCROWNED-KING` 획득 여부가 아니다. 개인 수상·대표팀 선발만으로 팀 트로피를
  부여하지 않으며, 분모가 1,000명 미만이면 해당 비율은 진단만 보고하고 판정 근거 부족으로 남긴다.
- 위 범위는 네 포지션을 동일 수로 합친 합성 참조집단에 적용한다. 포지션별 동일 비율을 강제하거나
  낮은 점수 행을 버리지 않는다. 포지션별 분포·요청/실현 길이·출전·5축 점수는 별도 공개하고,
  동일한 성과를 동등하게 평가하는 기존 포지션 공정성 검증도 함께 통과해야 한다.
- 허용 범위는 통계적 신뢰구간이나 실제 사용자 비율 추정치가 아니라 사전 지정한 제품 판단 기준이다.
  작은 파일럿에 이 범위를 assertion으로 적용하지 않는다. 최종 전체 표본에서 벗어나면 목표 미달을
  그대로 보고하고 원인·후보를 재검토한다. 분위수 역보정이나 결과를 본 뒤 허용 범위 확대는 하지 않는다.
- 최종 choice policy와 ruleset/Legacy 버전은 아직 미확정이다. random/opportunity/mixed 중
  유리한 결과만 공식 모집단으로 선택하지 않는다. 공개 UI에서 가능한 정책과 정책 구성비를 먼저
  문서화하고 checksum을 고정한 뒤 전체 재생성한다. 현재 v5는 진단 전용이며 발행 금지는 유지한다.

평균 점수 상승만으로 채택하지 않는다. 단기·벤치 경력이 무조건 고득점이 되지 않고, 같은 실제 성과의
포지션 간 점수 차이가 작으며, 좋은 선택·충분한 출전·관계 유지가 의미 있는 차이를 만드는지 확인한다.
모든 엔딩의 조건 fixture와 자연 실행에서 실제 발견한 엔딩 종류는 별도로 보고한다.

## 현재 미완료

- 새 정책 수식·버전 라우팅 확정 및 구현
- 성장/관계 규칙 변경 필요 여부를 결정할 paired replay
- 새 정책 파일럿·4만 커리어 결과·참조집단 배포
- 최종 head CI, main 병합 및 배포 확인

## 1차 후보 실험 — 채택 아님

`tooling/scripts/legacy-balance-probe.mjs`는 SHA로 고정한 기존 생성기를 계측하여 같은 커리어의
원본 평가와 후보 평가를 비교한다. 원본 번들을 수정하지 않으며 결과를 참조집단으로 발행하지 않는다.
후보는 기여의 커리어 평균/전성기 5시즌을 절반씩, 장기성을 실제 활동 연수로, 관계를 가장 강한
두 관계의 지속성과 전성기로 평가한다. 태그 서사는 COMMON/RARE/EPIC 8/20/35점 후보를 사용한다.
성장식이나 선수 능력치를 바꾼 실험이 아니다.

포지션별 seed 0..39, 요청 시즌 1..20 두 순환의 총 160개 비교:

| 포지션 | 기존 평균 | 후보 평균 | 후보 최고 | ICON/LEGEND |
| ------ | --------: | --------: | --------: | ----------: |
| GK     |     32.00 |     32.28 |        60 |       0 / 0 |
| DF     |     44.23 |     46.58 |        70 |       0 / 0 |
| MF     |     33.55 |     34.40 |        73 |       0 / 0 |
| FW     |     33.95 |     35.35 |        64 |       0 / 0 |

평균을 크게 올리지 않으면서 낮은 출전의 장기성 과대평가를 줄이는 효과는 있으나, 고득점 달성
경로와 분포 목표를 입증하지 못했다. **현재 후보를 최종 정책으로 승인하거나 활성화하지 않는다.**
도메인은 명시적인 `1.1.0` 후보 호출을 지원하되 기본값은 기존 `1.0.0`을 유지한다.
후보 정책의 최종 고정은 별도 인수 후이며, 일반 플레이에서 기존 결과를 자동 재평가하지 않는다.

### 선택 경로 진단

- 6개 20시즌 seed를 잔류 우선 정책과 비교하면 관계 점수는 모두 상승했지만 출전 시간은
  네 경로에서 감소했다. 잔류만으로 선발/성장 문제가 해결되지는 않는다.
- 전술식의 style/archetype 비중만 0.60/0.40에서 0.85/0.15로 바꾼 작은 paired 비교는
  GK·DF에서 오히려 출전을 줄였다. 이 일괄 변경은 채택하지 않는다.
- 같은 6개 seed의 시즌말 selection을 확인하면, 일부는 건강한 벤치지만 일부는 부상·복무가
  원인이었다. GK:127, FW:31은 경쟁자 OVR이 10~20점 높은 강팀에 오래 머물렀다.
  모든 낮은 출전을 선발식 오류로 취급하지 않는다. 시즌말 표본을 전체 경기 통계로 부르지 않는다.
- 기존 표본의 KEEP 거절은 실제 UI에서 선택할 수 없는 행동이었다. v3는 확인만 수행한다.
  같은 160개를 KEEP 확인으로 바꾼 후보 최고점은 GK 62 / DF 70 / MF 73 / FW 66이다.
  표본 정책 오류는 수정했으나 이 수정만으로 밸런스 목표를 달성했다고 보지 않는다.

다음 판단은 공개 제안 조건을 보고 출전 기회를 선택하는 경로와 임의 계약 선택의 차이를
작게 비교한 뒤 내린다. 검증 전 임의로 성장률·선발 보너스를 올리지 않는다.

출전 기회 우선 계약·같은 그룹 감독 역할 수락을 적용한 32개 20시즌 paired 비교에서는
기존 점수식 기준 평균이 GK 44.5→56.5, DF 64.8→67.3, MF 46.0→54.1, FW 50.3→55.1이었다.
평균 출전 비율도 각각 6.3→19.8%, 22.4→33.3%, 7.2→19.0%, 13.8→28.3%로 개선됐지만
개별 악화 사례가 있고 최고점은 74였다. 이 정책은 시장과 역할 선택을 함께 바꾸므로
시장만의 인과 효과나 최적 플레이의 상한으로 해석하지 않는다.
실제 플레이 선택의 대표성을 먼저 개선할 근거이며, 분포 목표 합격이나 신규 점수 정책 채택의 증거는 아니다.

## 테스트 유지 범위

사용자 지침에 따라 최소한의 필요한 검증만 유지한다.

- 상시 CI: 점수 범위·핵심 경계, 같은 입력의 재현성, 구버전 결과 보존, 은퇴 저장/복구와 핵심 화면 흐름.
- 밸런스 배치: 정책 확정 시 별도 실행하고 보고서·생성기 checksum으로 증거를 보관한다.
  4만 커리어를 매 PR마다 재생하거나 각 seed의 총점·세부 통계·엔딩을 영구 snapshot으로 고정하지 않는다.
- 배포 산출물: 생성·발행 시 원본 전체를 검증하고, 런타임에서는 형식·버전·무결성을 확인한다.
  같은 4만 행에 대한 수십만 개 assertion을 상시 단위 테스트에 중복 추가하지 않는다.
- UI: 핵심 동작·저장 여부 중심으로 검증하고, 사소한 문구·CSS 내부 구조·애니메이션 시간을 고정하지 않는다.
- 분포 목표는 밸런스 판단 지표다. 작은 무작위 표본의 정확한 비율을 CI 실패 조건으로 삼지 않는다.

관련: [통합 검증 기록](phase-5-verification.md), [기존 모집단 프로토콜](phase-5-population-protocol.md).
