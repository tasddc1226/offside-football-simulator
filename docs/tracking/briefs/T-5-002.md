# T-5-002: 불변 Archive 코어·Legacy 버전·저장 계약

요청: Phase 5 다음 작업 착수. Phase 3·4 충돌 회피와 병합 대기 유지.
선행: PR #70 / `32e4ae5`(T-5-001), main `a468837`.
브랜치: `T-5-002-archive-contract` — PR #70 위의 후속 브랜치. 선행 PR에 커밋을 추가하지 않는다.

## 범위

- 신규 `packages/domain/src/legacy/archive.ts`, `archive-legacy-binding.ts`, 해당 테스트·전용 fixture.
- Phase 5 내부 `legacy/index.ts`만 확장. 공통 domain export는 수정하지 않는다.
- [Archive 계약 명세](../../development/17-phase5-archive-contract.md), 이 브리프, Phase 5 계획 갱신.
- 기존 엔진·공유 타입·contracts·ruleset·content·웹·CI·DB·공유 보드·기존 golden 수정 금지.

## 요구사항 연결

- FR-LEG-001: 은퇴 기록 보관의 내부 계산·보존 기반. 공개 보관함까지 완료한 것은 아님.
- DATA-CAR-001, 저장·버전 정책: 생성 당시 serviceSeason/ruleset/pack 유지.
- Phase 5 Archive 불변 조건: 깊은 readonly·입출력 분리·hash·덮어쓰기 거부. 실제 DB 원자성은 미구현.
- RULE-LEG-002·004·006: Legacy 버전과 참조 분포를 별도 불변 평가 binding에 고정. 실제 LegacyResult 계산·엔딩 조건은 후속.
- API-CAR-004·API-LEG-001~003: 향후 저장·조회 경계와 인가·검증·트랜잭션 요구사항 명시. 라우트 구현 없음.

## 완료 조건

- [x] 확정 은퇴·버전·원본 hash·시즌/timeline 완전성 검사.
- [x] 코어 재구성 검증, 수정된 파생 통계·서비스 시즌·checksum 거부.
- [x] 입력 참조 분리·깊은 freeze·JSON 재개·반복 재시도 hash 검증.
- [x] INSERT/REUSE/conflict 순수 정책과 동일 Legacy 버전 변조 거부.
- [x] 실제 저장·인가·동시성·업적 수집 공백을 구현 범위와 분리하여 문서화.
- [x] 신규 및 기존 도메인 테스트, lint·의존성 경계·타입 검사, 범위 검사.

## 검증 기록

검증일: 2026-09-05. 테스트는 실제 결산 fixture에서 명시적으로 RETIRED 경계를 구성한다. 실제 RETIRE 명령이나 20년 실플레이를 검증했다고 표현하지 않는다. 테스트 artifact checksum은 레지스트리 fixture이며 운영값이 아니다.

| 실행                                                                 | 결과                                                                   |
| -------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile`                                     | 성공, 의존성·lockfile 변경 없음                                        |
| `pnpm --filter @offside/domain exec vitest run src/legacy`           | 5개 파일·108개 통과. T-5-001의 73개 + 이번 신규 35개                   |
| 전체 순차 검사 중 domain suite                                       | 52개 파일·744개 테스트 통과, 기존 golden 갱신 없음                     |
| `pnpm lint` / `pnpm lint:deps` / `pnpm typecheck`                    | 전체 통과                                                              |
| `pnpm exec turbo run test --concurrency=1 --output-logs=errors-only` | 10개 패키지 성공(1개 캐시), 192개 파일·2,114개 테스트 통과, 1분 46.3초 |
| `pnpm build`                                                         | API·웹 빌드 통과                                                       |
| 변경 범위·상대 문서 링크·`git diff --cached --check`                 | 선행 PR 대비 9개 파일, 허용 범위 내, 검사 통과                         |

20시즌 합성 코어는 256KiB 이내 크기 검사와 JSON 재검증을 통과했다. 실제 장기 플레이 데이터의 크기·전송 한도를 확정하는 근거는 아니다. T-5-002의 실제 저장 통합·동시성·브라우저 E2E는 미실행이다.
