# T-7-040: 중요한 결정에서 멈추는 서버 연간 커리어

## 범위와 승인

2026-09-22 사용자가 자연스러운 능력치·상황 기반 사건 흐름, 고정 재계약 일정 제거, 한 버튼으로 한 시즌/1년 진행, 실제 출전 시간에 따른 눈에 보이는 성장을 요청했다. 추가 답변은 **중요한 결정이 생길 때만 잠시 멈춤**이다. 모든 결정을 자동 수락하는 설계는 승인되지 않았다. Luna 배정 무응답 뒤 다른 하위 에이전트 구현을 사용자 `진행`으로 승인했다. AGENTS 자체는 변경하지 않는다.

- 기준 main `fc281bd5`, 브랜치 `T-7-040-annual-career`.
- 신규 후보 `3.5.0 / 0.14.0`. 역사 팩·규칙·저장 해시 불변. 최종 통합 전 활성 포인터 변경 없음.
- 최초 구현 승인에는 main 병합·운영 배포가 포함되지 않았다. 2026-09-23 사용자 `좋아 운영 환경까지 배포해줘`로 후속 릴리스를 승인했다. 필수 검사와 독립 검토를 통과한 뒤 병합·staging·운영 preflight·배포 순으로 진행하며 검사를 우회하지 않는다.
- 참고: CLAUDE.md, ADR-002/003/005, FR-GROW-1/4, DATA-GROW-1, RULE-TIME-004, DATA-SEA-001, CMD-SIM-003, CMD-CON-001~003.

## 2026-09-23 병합 후 staging 전파 검증 보강

- PR #276은 `aa79561e4111c69034ef570bbf4f9488ba538201`로 squash 병합했다. 병합 전 exact head
  `be09877`의 CI attempt2가 성공했고 comments/reviews/threads는 0이었다. offline runner21의
  중단된 CI는 승인된 취소 후 기존 repo 전용 runner22 `--once`로 재실행했으며 작업 뒤 정상 종료했다.
- 최초 main staging run `35808731464`는 migration/API/web 배포에 성공했으나 배포 직후 이전
  웹 자산이 응답해 UI가 새 ruleset을 인식하지 못했다. 후보 자산이 원격에 도달한 뒤 실제 bytes
  SHA256 일치를 확인했다. 이를 성공으로 덮지 않고 fail-closed readiness 후속 브랜치
  `T-7-040-release-readiness`를 병합된 main에서 만들었다.
- 공통 guard는 candidate shell의 JS/modulepreload/CSS 집합과 자산 hash를 2회 연속 검증한다.
  실제 staging에서 CI dist 기준 25개 자산×2회 통과했다. Production은 web 배포 뒤 CAS 전에
  이 gate가 실패하면 이전 생성 manifest를 유지한다. 전체 CDN 동시 전파나 열린 구버전 탭의
  갱신까지 보증하지 않는다.
- 후속 scripts 전체34/34(기존28+추가6), CI 단위9/9, scripts lint, web/E2E typecheck,
  의존 방향 검사 통과. 독립 guard6/6 및 source review 통과. 전체 패키지3,153개를 이 후속
  변경에서 다시 실행한 것은 아니다. 게임 runtime/버전 콘텐츠는 변경하지 않았다.
- 실제 원격 smoke는 생성201 이후 소유권 확인·정본 다운로드를 기다리도록 고쳤다. 60초 전체
  제한은 실제 첫 중요 결정까지 통과 후 reload 중 소진됐으므로 승인 후 이 원격 테스트만
  120초로 조정했다. 단계별45/30/20초, 실제 상태 단언, retry0은 유지했다. 최종2/2 통과.
  순차 네트워크 검증의 지연을 제거한 변경은 아니다.
- 독립 실제 staging 브라우저 Space8: 첫해 중요 결정5회, 같은 해 reload/키보드 선택/360px,
  자연 출전 기회 사건, 2,690분·OVR66→67·19→20세·20능력치 보고와 자동 다음 해 시작 없음
  확인. 관측하지 않은 네트워크 구간 전체를 무오류라고 일반화하지 않는다. 운영 배포는 아직
  진행하지 않았으며 새 후속 PR CI·main staging·production preflight 승인이 별도로 필요하다.

## 네 단계

### 첫 중요 결정 표시 경로의 불필요한 보고서 재조회 제거

- PR #277은 `25f615d4b0f323149d5b7c197106a79f3154b32a`로 병합됐다. main staging
  `35810281938`에서 새 readiness는 25자산×2회 통과했고 배포·health·manifest도 성공했다.
  UI smoke는 첫 중요 결정의 30초 단언에서 실패했다. 이를 gate 통과로 취급하지 않는다.
- trace에는 연간 POST/advance/current/canonical GET이 모두 성공했지만 화면이 `busy`인 채
  변경되지 않은 과거 보고서 GET과 앞뒤 owner 검증까지 기다리는 모습이 남았다. 마지막
  보고서 조회가 약4.5초 추가됐다. 이에 `T-7-040-pause-readiness`를 위 main에서 분기했다.
- runtime 수정은 `act()`가 반환된 run이 COMPLETED일 때만 history를 다시 읽도록 제한했다.
  초기 로드와 완료 시 보고서 갱신, 모든 owner/abort/revision/canonical cache 검증은 유지한다.
  WAITING/RUNNING의 변경 없는 과거 보고서 재조회만 제거했다. timeout은 추가로 늘리지 않았다.
- 새4회귀는 pause 시 deferred/실패 history 추가 호출 없음, 초기·완료 history 유지,
  owner 전환 뒤 이전 완료 응답 미노출을 검증한다. 집중14/14, 전체 web762통과/기존1opt-in
  skip(97파일), web/E2E typecheck·대상lint·build·bundle115.00KB 통과. 독립 소스 승인.
  실제 local API 연간 E2E도 독립1/1 통과(7.5초, retry0)했다. 원격 새 runtime의 30초 gate
  통과는 새 PR/main staging 결과로 별도 확인해야 한다.

1. **도메인/콘텐츠** (구현·작성자 검증·독립 검토 완료): 순수 고정 목표 연도 진행기, 모든 pending 분기, 자연 명령 재생; 출전 기회/역할 갈등/진로 관심의 저장된 후속 사건; 계약 만기 맥락 창; 실출전 성장과 전체 연도 변화 보고.
2. **권위 서버** (구현·작성자 검증·독립 검토 완료): 신규 버전 SERVER_ANNUAL 소유권, 시작/청크/선택/복구; 동일 작업 재시도·병렬 CAS·실행 로그와 snapshot 원자 저장. 과거 CLIENT_LOCAL 경로 보존.
3. **연간 UI** (구현·집중 회귀·독립 브라우저 검증 완료): 1년 보내기→중요 결정→같은 해 계속→연간 보고. 360px, 키보드, 모션 감소, 로딩·오류·재접속·중복 클릭.
4. **통합 검증** (2026-09-23 후속 gate 통과): 전체 패키지3,153통과와 기본 E2E118통과/기존 선택 실행8skip/실패0을 확인했다. 9월22일 draft 제한을 남긴 실패들을 해소했다. 최종 커밋 CI·독립 검토·병합·staging·운영 배포는 각각 별도 결과로 기록한다.

## 핵심 계약

- AnnualCheckpoint는 시작 revision/age/능력치/OVR/완료 시즌 수와 targetSeasonIndex를 고정한다. 부분 시즌 시작은 남은 시즌만 마치고 두 번째 해로 넘어가지 않는다.
- nextAnnualAction은 COMMAND/WAITING_DECISION/COMPLETED/ERROR 중 하나를 반환한다. 서버가 명령을 실행하고 예산 내 청크를 저장한다. HTTP 선택은 허용 choice ID만 받고 payload/outcome은 콘텐츠에서 재생성한다.
- 첫계약·이적·재계약·임대·은퇴·실질 역할 변경·중요 사건은 자동 수락하지 않는다. 빈 제안/KEEP 역할/일상 훈련·경기 장면은 정책으로 처리한다.
- 연간 보고는 시작과 종료 전체 속성 차이, 결산 성장과 기간 중 변화 차이를 함께 보여 준다. 결산 성장의 MINUTES/TRAINING/EXPERIENCE/AGE_DECLINE/POTENTIAL_CAP은 기존 사실 원장을 사용한다.
- 신규 버전 DEVELOP는 체력·숙련·관계에 영향을 주며 영구 능력치 상승은 결산에 한 번만 계산한다. 실제 출전 기여 0분은 0이고 소량 훈련, 연령·잠재력·연간 상한은 유지한다.
- 계약은 원래 1~3년이다. 매년 고정 재계약 버튼을 제거하며 실제 만기 계약에만 감독 신뢰에 따른 논의 시점을 둔다. 필수 만료 후 시장은 보존한다.
- 사건은 실제 감독 actor/구단/출전 기록과 최초 선택을 저장한다. 타 감독에게 기억을 넘기지 않는다. 동료 부상·확정 이적·확정 출전을 창작하지 않는다. 동일 창 재굴림 방지, 연간 새 줄거리 1개와 후속, 가족별 2시즌 쿨다운.

## Phase 0 기준선

`/tmp/offside-annual-baseline-01TIJp`: 3.4.0/0.13.0 FAST·opportunity, 4포지션×3시즌. 0분에도 45% 성장 바닥이 있고 DEVELOP 즉시 성장까지 합쳐 결산 이전 변화가 크다. 이 표본과 신규 자연 연간 명령의 출전·전체 OVR·중요 중단 횟수를 비교한다. 합성 성장 대조 실험과 자연 플레이 증거를 구분한다.

## 검증 기록

### 후속 릴리스 준비 — 2026-09-23 (진행 중, 아직 병합·배포하지 않음)

- PR #276은 `b3b64936ecccc8166c15ce15c22b13c03f004caf`의 draft다. 최초 quick CI는 self-hosted runner 연결 상실로 실행 단계 없이 실패했고, 같은 SHA 재실행 attempt2는 통과했다. 아래 후속 변경의 최종 CI 결과로 대신 사용하지 않는다.
- 기본 E2E의 역사적 실패도 배포 통과로 면제하지 않는다. 실제 대비·개발 probe 진입 결함은 런타임에서 수정하고, 과거 화면/버전·동기화 응답을 잘못 가정한 E2E는 실제 계약에 맞게 보정한다. 전체 E2E 재실행 전까지 이전 비정상 기록은 그대로 유효하다.
- 주소 고정 전에 최초 memory-history 경로를 보관하여 DEV-only hash probe를 복원했다. 운영 allowlist는 넓히지 않았다. 사건 시트와 결과 공개의 읽을 수 있는 내용은 위치/크기만 전환하여 중간 opacity의 대비 저하를 없앴고, 시즌 단계의 작은 글자 opacity를 제거했다. 시간·모션 감소·키보드 동작은 유지한다.
- 집중 검사: release guards10/10, web history/result/retirement45/45, 변경 TS/JS lint 및 web+E2E typecheck 통과. 실제 hash/axe/전체 E2E는 독립 검증자가 이어서 확인한다. 이는 전체 릴리스 통과 기록이 아니다.
- 독립 실제 검사에서 hash probe, 사건/대시보드 대비, 은퇴4화면 axe 통과를 확인했다. 스와이프는 native touch trace로 `enabled=true`인데 `main#game-content[tabindex=0]`을 내부 조작 요소로 오인해 시작을 거절함을 확인했다. 일반 focusable 요소 검사만 표면 내부로 한정하고, 바깥 form 등 의미 있는 조작 요소의 차단·본문 키보드 focus는 유지했다. 외부 main 허용/내부 focusable 거절/외부 form 거절을 포함한 swipe36/36 및 UI+web/E2E typecheck 통과. 실제 제스처 및 전체 E2E 재실행은 진행 중이다.
- 이후 실제 제스처2/2 통과, 집중17개 중16통과 뒤 남은 두 번째 터치가 보호된 summary 위에서 시작함을 확인해 테스트 시작점만 일반 문구로 옮겼다. 은퇴 전체6화면 axe·저장된 hash/DOM, 이적3개 및 장애 대응4개 검사도 통과했다. 기본126개 전체 첫 재실행은117통과/8기존 선택 실행 skip/1실패였다. 마지막3.2 챕터의 같은-route 훈련 표시 대기 누락을 helper에서 보완하고 역사3버전 집중3/3 통과했다. 제외 없는 최종126개 실행은 별도로 기록한다.
- 마지막 런타임/테스트 수정 뒤 frozen install 및 전체10패키지를 maxWorkers=1로 직렬 재실행해 **3,153통과/실패0/기존 web 선택 실행1skip**을 확인했다: domain1034/content508/contracts226/fixtures45/engine114/platform59/UI112/API269/scripts28/web758. API40파일194.89초, web97파일112.29초. SEO4/4와 CI scripts9/9도 통과했다. 로그는 `/tmp/offside-annual-release-gates.vduGhK`다.
- 최종 기본 E2E는 **126개 전체,118통과/8기존 선택 실행 skip/실패0/flaky0**,331.84초다. 어떤 실패 사례도 제외하지 않았다. 독립 실행 JSON `/tmp/offside-annual-release-validation.zL3oDF/default-e2e.json`의 집계를 작성자가 다시 확인했다. 건너뛴 항목은 실제 API 연간1, Google 연결2, 성능1, API 복구/충돌3, 실제 service-season1이며 기본 설정의 기존 opt-in 조건을 유지했다. 실제 연간 항목은 별도 API 실행으로 확인하며, 나머지 opt-in을 통과했다고 주장하지 않는다.
- 최종 실제 로컬 API 연간 E2E1/1 통과(7.39초), 새 build Worker의 `/onboarding`·`/career/qa-release-annual`200/noindex 및 미등록 경로404/noindex 통과. 실행 JSON은 같은 디렉터리의 `annual-local.json`이다. 임시 Worker8892만 정상 종료했고 기존5173/8891은 보존했다. 독립 검증자가 런타임 수정과 테스트 계약 보정을 승인했다.
- 정적·빌드 gate 통과: lint10(7cache), typecheck9(7cache; 변경 UI/web+E2E 재실행), 의존 방향, 콘텐츠1,401이벤트/384경고(내용 불변 cache), light/dark 대비 검사, build2(API cache/web 새 build), 초기 bundle115.01KB gzip/예산300KB, db:check drift 없음, 역사 JSON/fixture 변경·삭제 없음, diff 공백 검사. JSDOM scrollTo 경고·기존 route-test 탐지·큰 비초기 chunk 경고는 실패와 구분한다.
- 공개 운영 current는 아직 `3.4.0/0.13.0`이다. `3.5.0/0.14.0` 승격은 0014→API→web→정확한 manifest CAS 순서이며, rollback은 신규 생성 포인터만 되돌리고 연간 저장을 읽고 진행하는 새 API/web/schema를 유지한다. 상세는 [운영 런북](../../operations/production-release.md)을 따른다.
- 운영 배포 전 저장 보존 기준은 사용자 승인된 별도 신규 익명 QA 소유자로 만들었다. 실제 EngineClient의 과거3.4/0.13 CREATE/UPDATE/CONFIRM 및 buildSyncBody를 사용했다. 최초 PUT은 Origin 누락으로 route/idempotency 이전403에서 거절됐고, 같은 소유자·career·request key에 대한 명시 승인 두 번째 시도만 실행해 revision3/hash read-back을 확인했다. 기존 사용자 데이터·출판·시즌 진행은 건드리지 않았다. 구 API가 authority를 노출하지 않아 `LEGACY_API_UNDECLARED`로 기록했으며 DB authority를 증명했다고 주장하지 않는다. 배포 후 같은 소유자 GET의 hash/revision 및 명시 CLIENT_LOCAL 응답을 비교할 예정이다. 자격 증명은 git 밖0600 파일에만 보관하고 로그에 노출하지 않는다.

### Phase 4 — 2026-09-22 (구현 검증 완료, 기본 E2E 비정상으로 draft review)

- Phase3 커밋 `89c319d91f86138183408bfd8e66a846bbf5a6c9` push/원격 SHA 확인 완료. 앞선 실제 브라우저 증거는 이 소스 범위이며 이후 실패 경로 보강과 구분한다.
- 전체 web 첫 실행은 754개 중 728통과/25실패/1기존 skip이었다. 23실패는 역사 CLIENT_LOCAL 화면 fixture가 새 기본 버전을 사용한 것이므로 해당 테스트만 3.4.0/0.13.0으로 명시 고정했다. 기사 테스트 2개는 owner 의도 조회용 store mock을 보완했다. 운영 기본값·서버 권위 차단은 완화하지 않았다.
- 후속 검증에서 손상된 서버 캐시가 로컬 명령 재생으로 복구될 수 있는 경로를 차단했다. SERVER_ANNUAL/예약 버전은 검증 실패 시 로컬 simulator와 저장을 호출하지 않고 소유권이 확인된 canonical GET으로만 복구한다. 과거 로컬 복구는 유지한다.
- 삭제 요청이 `sync:pending-delete`에 남은 동안 canonical recovery가 서버 기록을 재수입하는 실제 회귀를 재현했다. 진입 조회 전에 차단하고, 연간 캐시 및 복구 대조의 두 import 경로에 같은 삭제 큐 키를 전달하여 write transaction 안에서 다시 검사한다. 이는 대기 큐 보호이며 성공한 삭제 이후 영구 tombstone 보장으로 표현하지 않는다.
- PageShell 안의 연간 화면이 중첩 main을 만들지 않도록 일반 컨테이너로 변경하고 단일 landmark 회귀를 추가했다. 독립 검토 승인 후 소스를 다시 동결했다.
- 집중 검증: web8파일91/91, controller6/6(진행 중 GET 사이 삭제 포함), engine import9/9(무쓰기 거절), reconcile20/20 통과. 변경 코드 lint, web+E2E/engine typecheck 통과.
- 최종 전체 패키지를 maxWorkers=1로 직렬 재실행했다: domain1,034, content508, contracts226, fixtures45, engine-client114, platform59, UI109, API269, scripts28, web758 통과. 합계3,150통과/실패0이며 web의 기존 `OFFSIDE_SLOW_TESTS` 20시즌 대표팀 도달성1개는 기본 실행에서 skip이다. API는 기존 node Vitest 설정 그대로 실제 D1 테스트를 실행했다(40파일/186.10초). Web은97파일/108.79초였다.
- 최종 SEO4/4, CI scripts9/9, 전체 lint10/10(8cache), 전체 typecheck9/9(6cache, 변경 engine/platform/web+E2E 재실행), 의존 방향, build2/2 통과. 초기 bundle115.00KB gzip/예산300KB, db:check 새 schema 없음. 큰 비초기 엔진 chunk·route-test 탐지 경고는 남아 있다. 기본 E2E와 실제 서버 E2E 결과는 아래처럼 분리한다.
- frozen install, 전체 lint10패키지, 의존 방향 검사, 전체 typecheck9패키지, 전체 콘텐츠 검증(1,401이벤트·384경고) 통과. 최종 실패 경로 변경 뒤 다시 필요한 검사를 수행한다. 역사 packs/rulesets/fixtures의 기존 파일 변경/삭제는 기준 main 대비 없으며 diff 공백 검사도 통과했다.
- 최종 hardening 소스에서 실제 로컬 연간 E2E1/1 통과(8.9초), 새 build의 Worker 연간 deep-link200/noindex·미등록 경로404/noindex를 독립 검증자가 확인했다. 기본 E2E bounded 진단은34통과/5실패/1중단/1skip/85미실행으로 전체 통과가 아니다. 기존 이벤트/대시보드 대비2건, 물음표 제목 정규식2건, 법적 안내 edge-back1건은 별도 기준 main `fc281bd5`의 변경 없는 테스트에서 재현했다(4건 결정적 재현, 이벤트 대비는 반복3회 중1회). 이5건은 통과로 계산하지 않는다. 다음 실행은 정확한5건을 제외한 나머지121개를 대상으로 했다.
- 기준 재현 대상은 `a11y.spec.ts:259,300,363,373` 및 `app-motion.spec.ts:239`다. 로컬 증거는 `/tmp/offside-annual-e2e-default.4G2tdn/{test-results,baseline-five,baseline-event-repeat}`에 있다. 첫 기준 실행1통과/4실패(40.5초), 이벤트 단독3회 반복은1실패/2통과(18.8초)였다. 현재와 기준 모두 같은 이점 목록 노드의 대비 기준 미달을 확인했으며, 색상 전환 시점에 따른 값 차이는 통과 근거로 취급하지 않았다.
- 후속121개 실행은94통과/16실패/8skip/2중단/1미실행에서 제한 종료했다. 앞서 제외한5개는 이 집계에 들어 있지 않다. 따라서 기본 E2E 전체는 비정상 상태이며3,150개 단위/통합 테스트 통과와 구분한다. 실패 중 simulator-rebuild의 사용자 정의2.0.0/0.7.0 fixture를 공유 생성 helper가 덮어쓴 테스트 변경 회귀를 발견해 WeakSet 명시 등록으로 수정했다. 메타데이터·기대값·실제 게임 코드는 바꾸지 않았다. 해당 테스트 재실행 및 추가 기준 테스트 최대10개·5분 조사 결과는 별도 기록한다.
- Simulator targeted 재실행은 원래 region 및 정확한2.0.0/0.7.0 기대값을 통과해 fixture 수정 효과를 확인했지만, 뒤의 기존 `선수 성장` label 기대값에서 실패했다. 테스트 전체 통과가 아니며 기대값은 완화하지 않았다. 원래 줄 번호가 import 추가로 바뀌어 첫 선택 실행은 테스트0개였고, 실제 증거는 제목 grep으로 재실행한 `simulator-fixed` 아티팩트다. 최종 E2E typecheck/lint는 통과했다.

후속121개 실패 분류(최종 기준 재현 결과):

| 테스트 | 현재 실패 지점 | 기준 재현 상태 |
| --- | --- | --- |
| app-motion:247 | 탭이 시즌에 머무름 | 미확정 |
| chapter:83,3.2 | readiness false 60초 | 미확정 |
| hash-probe:52 | 개발 probe 대신404 | 동일 실패 기준 재현; 주소 고정이 bootstrap 경로 검사보다 먼저 실행되는 기존 소스 확인 |
| injury:137 / mobile-refresh:287 / presentation:65 / retirement:23 / session-length:191 | 시즌 시작 버튼 대기 | retirement 동일 실패 기준 재현, 나머지4개 미확정 |
| nineteen-kickoff:44,160 | 20세 텍스트 정규식 불일치 | 2개 동일 실패 기준 재현 |
| presentation:17 두 경우 | radio3개 기대에2개 | 2개 동일 실패 기준 재현 |
| resilience:157 | event 경로 기대에 career root | 동일 실패 기준 재현 |
| sync:19,126 | 저장 중에서 완료 배지 없음 / intro timeout | 126 동일 실패 기준 재현; 19는 기준 실행이 앞선 background 단계에서 실패하여 동일 원인 미확정 |
| simulator-rebuild:13 | 수정 후 기존 성장 label 기대값 | 동일 실패 기준 재현 |

- 추가 기준10개는5분 한도 내 정상 종료(약3분),10실패 중9개가 현재와 같은 지점이고 sync:19는 더 이른 단계에서 실패했다. 초기5개와 합쳐 발견된21실패 사례 중14개 기준 재현,7개 미확정이다. 미확정7개를 기존 버그라고 단정하거나 면제하지 않는다. 기본 E2E 비정상 상태를 유지한 채 구현 결과를 draft PR로 전달하며 병합·배포는 하지 않는다.
- 추가 증거는 같은 로컬 루트의 `remaining-121`, `simulator-fixed`, `baseline-ten`이다. 기준 임시 worktree는 변경 없음 확인 후 제거했고 테스트 포트5176도 종료 확인했다. 독립 검증자의 임시 Worker8892는 승인 후 정상 종료했고 포트 폐쇄를 확인했다. 실제 연간 검증용5173/8891은 보존했다. 사용자 데이터는 삭제하지 않았다.
- Sync:19 읽기 전용 감사에서3.4.0/0.13.0 CLIENT_LOCAL은 새 권위 차단에 걸리지 않으며 SyncClient/use-sync/SyncBadge/career-actions는 기준과 동일했다. 실패 화면은 배지 숨김이 아닌 `저장 중`이었다. 항상revision1을 응답하는 mock과 CREATE+UPDATE_DRAFT의revision2 사이 불일치가 원인 후보지만, 정확한 기준 브라우저 재현이 없으므로 가설로 남긴다.

### Phase 3 — 2026-09-22 (독립 브라우저 검증 승인)

- Phase 2 커밋 `a8a3ced17fdd1c7cd410ece0e7c0ff0b758ce32e`에서 UI를 구현했다. 새 버전 생성은 완성된 draft와 최초 프로필 의도를 서버에 보내며 seed/능력치를 클라이언트가 결정하지 않는다. 스타일 선택 전 가짜 확정 수치를 표시하지 않는다. 서버 활성 버전이 과거 버전인 전환 구간에는 기존 생성 경로를 유지하고, 신규 버전 로컬 CREATE는 엔진에서 거절한다.
- 연간 화면은 한 번의 `1년 진행` 뒤 같은 작업의 RUNNING만 자동 청크 처리한다. 중요한 결정은 실제 계약 조건/상황과 동일 연차 계속 진행 안내를 표시한다. 완료가 다음 해 자동 시작을 뜻하지 않는다. 서버 currentStep/처리 명령 수만 표시하며 허위 진행률·닫힌 탭의 백그라운드 실행을 약속하지 않는다.
- 모든 연간 변이는 의도별 안정 키, owner/career/job 경계, 서버 정본 GET을 사용한다. 과거 receipt snapshot을 그대로 import하지 않는다. import 트랜잭션은 원래 profile ID와 연간 revision 단조성을 확인한다. 결정과 캐시 career revision이 정확히 맞지 않으면 이전 상황의 선택지를 숨기고 재조회한다.
- SERVER_ANNUAL은 legacy deep-link에서도 기존 명령 화면을 마운트하지 않는다. 은퇴·읽기 전용 화면도 서버 소유권을 확인한 뒤에만 보여 준다. 빈 로컬 저장소의 서버 커리어는 GET으로 복구하며, 로컬 execute/CREATE/sync body와 재전송 큐에서 서버 모드를 차단한다. 복구·도전 import도 최초 owner를 고정한다.
- 보고서는 연도 시작/끝 OVR·실제 출전 시간·전체20능력치 변화, 정산 및 정산 외 차이, 실제 정산 기여 원장, 저장된 actor/사건 결과와 지난 해 보고를 사용한다. 독립 회귀가 자연 명령에서 얻은 실제 AnnualReportSchema DTO를 렌더하여 nested result 경로와 모든20행을 확인했다.
- 활성화 변경은 별도 에이전트가 맡고 UI 작성자가 독립 검토했다. 후보3.5.0/0.14.0, 실제 운영 직전 버전3.4.0/0.13.0의 정확한 비교/역변경 조건을 유지한다. 역사 mock 버전과 과거 JSON은 보존한다. 변경된 파일은 배포 후보 설정이며 원격 활성화·배포 증거가 아니다.
- 소스 동결 전 집중 검증: web4파일28/28(controller/reconcile/독립presentation·recovery), engine-client import+engine23/23, Dexie15/15 통과. web+E2E/engine-client/platform typecheck, 변경 web lint/engine-client lint/platform 대상 lint, lint:deps 통과. Vite build 통과, 초기 bundle115.01KB gzip/예산300KB. 기존 route-test 탐지 및 큰 비초기 chunk 경고는 남아 있다. 역사 packs/rulesets/fixtures 바이트 diff 및 git diff --check 통과.
- 독립 실제 브라우저 검증 승인: local web5173→API8891 test cohort, 새 서버 선수 생성201, 첫해 실제 중요 결정4회→2580분/OVR63→67/19→20세, 전체20속성 응답 확인, PUT 없음. WAITING 새로고침 유지, 실제 선택 commit 뒤 HTTP 응답 유실을 주입한 다음 canonical GET으로 다음 결정 복구(중복 POST 없음), 명시적2년차 시작 응답 유실 뒤 RUNNING 새로고침/수동재개→동일jobID·2년차 실제 부상 결정까지 확인했다. 모바일 스크린샷 넘침 없음, 키보드 Enter·모션 감소 확인. 이는 한 seed의 자연 브라우저 표본이며 성장/중단 횟수 보편 보증은 아니다. 중복 제목·본문과 표 간격 개선 의견은 비차단으로 기록했다.
- 전체 테스트 체인, 배포형 Worker allowlist/private noindex, 운영 릴리스는 아직 증명하지 않았다. Phase4에서 로컬 검증을 이어간다.

### Phase 2 — 2026-09-22

- Phase 1 커밋 `cab76da2f1aa7914b0f0de228c57f2ee2ff4ec8c`를 작업 브랜치에 push하고 원격 SHA를 확인했다. Phase 2 독립 검토자는 소스·권위/원자성·240명령 경계와 역사 바이트 보존을 승인하고 annual/career-publications/careers/migrations 4파일 41/41을 별도 재실행했다(70.57초). 이 승인은 서버 범위이며 UI·최종 통합·운영 배포 승인이 아니다.
- 신규 HTTP 계약은 `packages/contracts/src/annual-career.ts`, 라우트는 `apps/api/src/routes/annual-career.ts`. 생성은 필수 expectedProfileId와 서버 seed/초기 명령을 사용한다. 중요한 선택은 choiceId만 받고 정본 콘텐츠로 명령을 재생성한다. 버전 3.5.0/0.14.0만 서버 모드이며 활성 시즌이 이전 버전이면 명시적으로 생성을 거절한다.
- 생성 receipt는 소유자별 분리, 시작은 career/startRevision 유일성, 청크는 run/fromRevision 유일성과 fresh session/owner/CAS claim을 사용한다. 영수증·상태·명령·체크포인트·은퇴 아카이브가 같은 D1 batch에 저장된다. 조건부 UPDATE 0행에 의존하지 않고 모든 후속 쓰기를 새 claim ID로 제한한다. 기존 PUT의 already-applied 반환 전에 서버 권위 경로를 거절한다.
- 자동 진행은 요청당 최대 4명령, 연간 안전 한도 240명령이다. 브라우저는 RUNNING 동안 다음 요청을 보내며 탭을 닫은 동안 서버가 계속 실행된다는 보장은 없다. 이전 receipt는 원래 revision 응답을 재생하므로 UI는 owner 및 career/job revision으로 오래된 응답을 차단해야 한다.
- migration 0014는 생성기로 추가했으며 역사 migration/snapshot은 수정하지 않았다. ADR-012는 ADR-003의 신규 서버 모드만 대체한다. 은퇴 artifact registry에 신규 팩만 추가했고 역사 entry와 운영 활성 3.4.0/0.13.0은 보존했다.
- 실제 D1 작성자 검증: annual/career-publications/careers/migrations 4파일 41/41 통과. 트랜잭션 말미 실제 NOT NULL 실패 SQL로 전체 rollback, commit 직전 session 폐기·소유자 변경, merge 후 접근, cascade 삭제, 클라이언트 위조 PUT, 다른 소유자의 같은 키, 병렬 청크 중복 방지를 검사했다. 자연 FAST 명령 진행 후 도메인이 실제 제시한 RETIRE를 테스트 정책이 선택해 아카이브→공개 기사→다른 소유자 도전→연간 진행까지 검증했다(자동 강제 은퇴 증거가 아님).
- 실제 로컬 Wrangler/workerd HTTP 측정 스크립트 `tooling/scripts/annual-worker-benchmark.mjs`: FAST 3년, 19청크, 중요한 결정 11회, 줄거리 1개. 최대 4명령/청크, HTTP 응답 84,248 bytes, wall p50 45.7ms/p95·max 65.7ms. 로컬 observability D1 span의 요청 trace당 최대 SQL 14개, batch 최대 8개. 로컬 DB 저장 checkpoint 최대 712 bytes, 시작 snapshot 최대 27,128 bytes. seed별 변동이 있는 한 표본이며 운영 CPU/메모리/요금 한도 보증은 아니다. 실제 메모리와 운영 CPU는 측정하지 않았다.
- 로컬 측정은 127.0.0.1:8891, 이 작업 트리의 local D1과 test cohort `annual_worker_bench`만 사용했다. 원격 DB·운영 배포·활성 포인터는 변경하지 않았다. 최종 UI 실제 브라우저와 전체 통합은 다음 단계다.
- 최종 작성자 회귀: annual 6/6(허용되지 않은 선택 422, 오래된 revision 409, 같은 키 다른 본문 409, 자연 중요 결정에서 합성 commandCount=240의 쓰기 거절 포함), 기존 friendlies/profile/profile-recovery 42테스트 통과. API/contracts typecheck, API/contracts/scripts lint, contracts 226/226, retirement-artifacts 8/8 통과. migration 재생성은 변경 없음. 역사 팩·규칙·fixture diff 및 공백 검사 통과. 240 경계 카운터 테스트는 자연 240명령 표본이 아닌 안전 경계 실험이다.

### Phase 1 — 2026-09-22

- 독립 검토 승인: 최종 자연/경계 6테스트 재실행 통과(5.45초), 역사 fixture determinism/golden decision 7테스트 통과, diff 공백 검사 통과. 소스 검토에서 발견한 첫 계약 거절·시장 대기 중 은퇴·교차 시즌 분 비교·허위 합의 문구·임시 효과 역보정·대표팀 중복 컨디션 위험을 수정 확인했다. 서버/UI/배포 승인이 아니라 Phase 1 범위 승인이다.

- 신규 타입: `startAnnualRun` / `nextAnnualAction`, 콘텐츠 정본 어댑터 `buildAnnualContentContext`. HTTP 계약/DB는 다음 단계 소유다. 보고서에 전체 연도 속성 변화뿐 아니라 실제 사건 선택/결과 ID 영수증과 해당 기간 줄거리를 제공한다.
- 안전한 사건 경계: 부상 치료/대표팀 결정 직후에는 이야기를 끼우지 않고 원래 같은-step 처리를 먼저 마친다. 챕터/훈련 직후 삽입은 `resumeStep/resumeReason`을 저장해 아직 뛰지 않은 동일-step 경기를 빠짐없이 1회 실행한다. 자연 테스트에서 발견한 24/30 라운드 누락을 수정하고 모든 실제 일정과 누적 분을 대조했다.
- 자연 연간 검증 `pnpm --filter @offside/scripts exec vitest run annual-career-natural.test.mjs --maxWorkers=1`: 최종 6/6 통과. 4포지션×3시즌, 모든 실제 제시 선택 실행 가능, 체크포인트 직렬화/재개 동일, 한 번 결산·나이+1, 부분 시즌은 다음 해로 넘어가지 않음, 전체 속성 델타 정합성. 자연 조건으로 세 가족 모두 노출되었으며 성공한 출전 기회 선택이 실제 선발 계산 입력인 감독 신뢰/선발 점수에 영향을 주는 것까지 검증했다. 출전 시간을 보장한다는 뜻은 아니다.
- 통제 대조(자연 표본과 구분): 동일 19세 FW 능력치/잠재력 여유/평가 경기 수 0에서 0/30/900/2400분 → OVR +0/+0/+2/+4. 연령·잠재력 상한 별도 검증. 기간 중 DEVELOP의 직접 영구 상승은 0이며 결산 성장과 이중 계산하지 않는다.
- 자연 첫해 중단은 4~6회(입단 사건 2회+첫 계약 1회 포함), 이후는 연간 2~4회였다. 이는 표본 결과이지 모든 seed의 상한 보증이 아니다. 부상·실제 계약 제안은 중요한 결정으로 계속 멈춘다.
- 자연 3년 OVR: GK 66→67→67→67, DF 63→64→65→65, MF 59→62→65→67, FW 59→62→66→69. 자연 표본은 기존 Phase 0과 seed/빌드·선택이 같지 않아 두 집단 차이를 성장식만의 인과 효과라고 주장하지 않는다.
- 상세 새 표본: `/tmp/offside-annual-natural-6wHZNC/{cohort,growth,coverage}.json`. 이전 실행 동일 표본 `/tmp/offside-annual-natural-Xm0j9i/`도 존재한다. 교차 시즌 출전 누계와 강제 부상/대표팀 삽입 금지 검증은 명시된 경계 단위 테스트이며 자연 대표팀→서사 삽입 증거로 표현하지 않는다(그 삽입 자체를 금지했다).
- 마지막 도메인 변경 후 전체 domain 74파일/1,034테스트·fixtures 15파일/45테스트 통과. content 33파일/508·contracts 8파일/226 통과, domain/content/contracts typecheck·lint·의존 방향 통과. 최종 통합 검증은 Phase 4에서 다시 수행한다.
- 콘텐츠 검증 `content:validate --pack 0.14.0` 통과(기존 복사 이벤트 경고 41개). Pack checksum `25d92284f471a758079c5e87fc72f1d01b628e5aa1f454580ee5cc90228c5395`, ruleset checksum `9a18e0c4ec025b28b383fdb187ee5841af5b4a9f229392ecfa7c96299cc415b5`.
- `git diff --exit-code HEAD -- packages/content/packs packages/content/rulesets packages/fixtures`가 0으로 역사 추적 파일 바이트 불변을 확인했다. 새 팩과 0.13.0의 비교는 manifest와 신규 사건 6개만 다르며 나머지 복사 파일은 동일하다. 새 버전 활성 포인터·운영 데이터는 변경하지 않았다.
