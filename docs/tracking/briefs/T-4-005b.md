# T-4-005b: Phase 4 화면 묶음 (b) — SCR-021 슬럼프·SCR-018 라커룸·SCR-016 윤리·위기·SCR-024 SNS·평판 (web)

> 부모 브리프 [T-4-005](T-4-005.md)의 D-59 분할 3묶음 중 둘째. 화면 요구는 부모 §2(SCR-021·018·016·024 항목)를 그대로 따른다. **D-61(2026-09-05)**: 새 테스트 코드를 쓰지 않는다(부모 §4 무효). "외부 전송 금지(테스트로 금지)"는 코드 리뷰로 확인한다 — `navigator.share`·`window.open`·외부 `href`를 쓰지 않는다.

## 선행

- **T-4-005a 머지 뒤** 시작한다(`apps/web/src/routes/-phase4/bodies.ts` 존재로 확인). T-4-009의 `labels.ts`(`relationTierLabel`·`managerTrustTierLabel`·`relationshipDirectionArrow`·`popularityTierLabel`·`effectExpiresAtLabel`)를 쓴다.

## 1. 구조

- 본문 파일 4개를 새로 만든다: `routes/-phase4/slump.tsx`(SCR-021), `locker-room.tsx`(SCR-018), `ethics.tsx`(SCR-016), `media.tsx`(SCR-024). 각각 (a)가 정의한 `Phase4BodyProps`를 받는 컴포넌트다.
- `routes/-phase4/bodies.ts`에는 **import 4줄과 `PHASE4_BODIES` 항목 4개만** 추가한다. 디스패처(`career.$careerId.event.tsx`)·`career-route.ts`·`event-screen.tsx`는 건드리지 않는다(요구가 맞지 않으면 questions에 적는다).
- 디자인 계약은 `docs/design/mobile-game-refresh.md`. 위험·예상 효과는 콘텐츠 `previewEffects` 라벨만 쓰고 결과를 계산하지 않는다. SCR-021은 Base OVR 감소 문구·수치를 어디에도 표시하지 않는다.

## 2. 인수 조건

- 전체 체인: `… && E2E_PORT=5233 pnpm --filter @offside/web e2e`(부모와 같은 명령, 기존 e2e만).
- PR 본문: `PHASE4_SEEDS`에 있는 presentation은 DEV 팩 오버라이드 + seed로 실제 도달한 360px 스크린샷(일회성 스크립트, 커밋 금지), 없는 것은 "미도달"; 번들 증가분; 콘텐츠 후속 표.

## 범위·작업 규칙

- 변경 허용: `apps/web/src/routes/-phase4/slump.tsx`·`locker-room.tsx`·`ethics.tsx`·`media.tsx`(새 파일), `apps/web/src/routes/-phase4/bodies.ts`(항목 추가만). `packages/ui`는 새 컴포넌트 추가만. **금지**: 그 외 전부.
- 서브에이전트·병렬 fork·`/simplify` 금지. `docs/tracking/worker-brief-template.md` 제약을 따른다.
- 브랜치 `T-4-005b-relation-screens`, 커밋 `T-4-005b: ...`. `gh pr create`가 막히면 push + `PR_BODY.md` + `DONE <sha>`로 멈춘다.
