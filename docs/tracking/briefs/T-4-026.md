# T-4-026 — Phase 3·4 잔여 이슈 종료 조건 보강

T-4-025 다음 묶음. 기존 시뮬레이션 결과·hash·콘텐츠 규칙은 바꾸지 않는다.

## 감사와 변경

| 이슈 | 기존 main에서 확인한 상태 | 이번 처리 |
| --- | --- | --- |
| #56 | 결산 CTA 및 deep-link 가드는 해결. 오래 열린 시즌 준비 화면에서 MARKET_OPEN 오류를 일반 재시도로 처리 | 오류 후 캐시를 무시해 최신 저장 상태 조회, 실제 pending 화면으로 이동. 기존 INTEREST E2E를 결산 CTA 직접 진입으로 변경 |
| #61 | 임대 제안·조건·복귀 선택·결과·진행 경로 구현됨 | 기존 E2E 및 도메인 검증 후 근거와 함께 이슈 종료. 중복 구현하지 않음 |
| #62 | 약속 위반 후 최종 신뢰와 result/history/hash 정합성은 해결 | 출전 약속 구역에 해당 룰셋의 기본 신뢰 조정값 설명. 전체 시즌 변화에 포함되며 하한 clamp로 실제 감소는 다를 수 있다고 명시 |

#62는 전체 시즌 신뢰 차이를 벌점으로 역산하지 않는다. 기존 결과의 재계산이나 새 저장 필드는 없다.
화면의 입력/결과 피드백은 Emil 디자인 원칙을 적용하되 별도 애니메이션·레이아웃 변경은 없다.
Luna가 표시·안내를 수정했고, 주 에이전트가 의미와 경계를 검토했다.

## 필요한 검증만 유지

- 기존 start-season / career-route / season-result-view: 55개 통과.
- 기존 약속 위반 결산 도메인 회귀: 최종값·history·hash 검사 통과.
- INTEREST E2E: 결산 → 다음 시즌 CTA → 시장 → 잔류 → 프리시즌 통과.
- T-4-025에서 임대 E2E 및 관련 도메인 회귀도 재검증 완료.
- Web TypeScript 통과. 신규 검증은 원인 규칙값과 전체 변화 구분 한 건 및 기존 E2E 경로 수정으로 한정.

## 작업 경계

해시 probe(T-4-017), 대표팀 seed/캡처(T-4-023), elapsedSec(T-4-024)의 파일은 수정하지 않는다.
기본 팩 0.1.0과 기존 커리어 기록은 유지한다. 확장 QA 배포는 T-4-027에서 분리한다.

Closes #56
Closes #62
