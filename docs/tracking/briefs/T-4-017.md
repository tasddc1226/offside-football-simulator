# T-4-017: Node/workerd 해시 probe에 career-12·career-13 fixture 추가 (api 테스트)

> **2026-09-05 20:05 재개(D-62·D-65)**: D-62가 D-61의 일괄 금지를 대체했고 사용자가 이 게이트를 이 세션에서 진행하기로 했다. 최신 `origin/main`(T-4-022 통합 `a2354a5` 이후)에서 시작한다 — career-12·career-13 골든은 T-4-022가 재기록했으므로 그 값을 기준으로 한다.

## 목표

T-4-006(domain, PR #72) 워커가 범위 밖으로 남긴 항목: `apps/api/src/test/cross-runtime-hash.test.ts`의 Node/workerd 해시 비교 순회가 career-11까지만 포함한다. career-12-injury(T-4-002)와 career-13-integration(T-4-006)을 추가해 브리프 T-4-006 §1(d) "Node/workerd 해시 probe가 이 fixture로 통과"를 닫는다.

## 참조·선행

- 정본: [T-4-006](T-4-006.md) §1(d), [T-2-006](T-2-006.md) API-CAR-003.
- 선행: 없음(PR #72·#73·#85 모두 main). `packages/fixtures/src/__fixtures__/career-12-injury.*`·`career-13-integration.*` 존재.
- 코드: `apps/api/src/test/cross-runtime-hash.test.ts:257~320`(fixture 표), `apps/api/src/test/hash-probe.worker.ts`, `packages/fixtures/src/index.ts`.

## 1. 작업

- 표에 `career-12-injury`·`career-13-integration` 항목을 기존 항목과 같은 형태(label·runOnNode·probeRequest·golden)로 추가한다. 두 fixture가 `packages/fixtures`에서 export되지 않으면 export만 추가한다(fixture 내용 변경 금지).
- 두 항목이 Node와 workerd에서 같은 stateHash를 내는지 확인한다. 불일치가 나면 고치지 말고 PR 본문에 재현 값(양쪽 hash·revision)을 적고 BLOCKED로 멈춘다.

## 2. 인수 조건

- 전체 체인: `pnpm install --frozen-lockfile && pnpm lint && pnpm lint:deps && pnpm typecheck && pnpm content:validate && pnpm test && pnpm build && pnpm --filter @offside/web check:bundle && pnpm --filter @offside/ui check:contrast && E2E_PORT=5227 pnpm --filter @offside/web e2e`. api 테스트는 단독 재실행(`pnpm --filter @offside/api test`)으로도 통과.

## 범위·작업 규칙

- 변경 허용: `apps/api/src/test/cross-runtime-hash.test.ts`, `packages/fixtures/src/index.ts`(export 추가만). **금지**: 그 외 전부.
- 서브에이전트·병렬 fork·`/simplify` 금지. `docs/tracking/worker-brief-template.md` 제약을 따른다.
- 브랜치 `T-4-017-api-hash-probe`, 커밋 `T-4-017: ...`. `gh pr create`가 막히면 push + `PR_BODY.md` + `DONE <sha>`로 멈춘다.
