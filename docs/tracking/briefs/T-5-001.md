# T-5-001: 충돌 없는 Phase 5 계산 기반

요청: Phase 3·4와 겹치지 않게 Phase 5에 착수하고 고도화 실행 계획을 포함한다.
기준 main: `a468837` · 브랜치: `T-5-001-career-foundation`

## 목적·요구사항

- FR-LEG-001의 통산 기록·Legacy 구현을 위한 순수 계산 기반을 만든다.
- RULE-LEG-002: 정규화된 5축 점수의 가중합·정수 반올림·밴드 경계.
- RULE-LEG-003의 일부 기반: 동일 시즌 증거 중복 집계와 임대 원소속 중복 집계를 막는다. 전체 사건별 점수 중복 방지는 미구현.
- RULE-LEG-006: 이미 충족한 엔딩의 우선순위·대안 2개·폴백 제외·표시 선택 분리.
- TEST-LEG-001·008·009의 하위 계산 단위 검증. 완전한 Archive·LegacyResult·UI 통합 검증을 대신하지 않는다.

## 허용 범위

`packages/domain/src/legacy/**`, 이 브리프, `docs/tracking/phase-5-plan.md`, `docs/development/16-advancement-execution-plan.md`만 추가한다. 기존 파일·golden·공통 barrel·스키마·ruleset·lockfile·화면·CI·공유 보드는 변경하지 않는다. 새 의존성을 추가하지 않는다.

## 산출물

- `career-records.ts`: 확정 시즌의 통산·구단·포지션별 통계와 원본 hash 참조. 입력 정합 검증, 중복 방지, 정수 overflow 거부.
- `score.ts`: 이미 정규화된 점수만 계산. 현재 점수 범위 0~100 유지.
- `endings.ts`: 카탈로그 ID·우선순위·충족 엔딩 해소·표시 후보 선택.
- 각 모듈의 단위 테스트와 실제 결산·임대 fixture를 읽는 통합 테스트.
- 고도화 실행 계획의 저장소 사본과 Phase 5 후속 순서.

## 완료 기준

- [x] 출전/결장·평점 가중·임대·포지션 전환·GK 음수 통계 테스트.
- [x] 빈 기록·중복·충돌·수치 오류·hash 변조·입력 불변·반복 hash 테스트.
- [x] 5축 가중치·밴드 전체 경계·반올림·부적합 값 테스트.
- [x] 엔딩 순서·중복·폴백·후보 2개·표시 선택 불변 테스트.
- [x] 기존 도메인 회귀 및 타입·lint 검증; 기존 fixture hash 변경 없음.
- [x] 변경 파일이 허용 범위에만 존재.

## 검증 기록

검증일: 2026-09-05. 20시즌 합성 집계는 실제 20년 엔진 완주가 아니다. 서버 저장 Archive, 은퇴 명령, Legacy 정규화/참조 분포, 엔딩 eligibility, SCR-025~028과 브라우저 E2E는 이번 작업 범위 밖이다.

| 실행                                                                 | 결과                                                                                      |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile`                                     | 성공, lockfile·의존성 변경 없음                                                           |
| `pnpm --filter @offside/domain exec vitest run src/legacy`           | 신규 3개 파일, 73개 테스트 통과                                                           |
| `pnpm --filter @offside/domain test`                                 | 최종 코드 단독 재실행: 50개 파일, 709개 테스트 통과(신규 73 + 기존 636), golden 갱신 없음 |
| `pnpm lint` / `pnpm lint:deps` / `pnpm typecheck`                    | 전체 통과. 최종 집계 함수 정리 후 domain lint·typecheck 추가 재실행 통과                  |
| `pnpm content:validate`                                              | 기존 팩 0.1.0·0.2.0, ruleset 1.0.0 통과, 경고 0                                           |
| `pnpm build`                                                         | API·웹 빌드 통과                                                                          |
| `pnpm exec turbo run test --concurrency=1 --output-logs=errors-only` | 전체 10개 패키지 성공(1개 캐시 사용), 190개 테스트 파일·2,079개 테스트 통과, 1분 49.6초   |
| `git diff --cached --check` / 상대 문서 링크 검사                    | 통과, 신규 10개 파일만 변경                                                               |

전체 병렬 `pnpm test`의 최초 실행은 기존 `fixture-determinism.test.ts`의 1,000회 재생 시간 조건에서 **11,775ms > 10,000ms**로 실패했다. hash 불일치가 아니라 시간 assertion 1건이며, 이로 인해 전체 실행이 중단됐다. 단독 도메인 재실행은 709개 모두 통과했다. 이후 패키지별 순차 실행은 위 표처럼 2,079개 모두 통과했다. 기존 테스트 기준·timeout·CI 설정은 바꾸지 않았으며, 이 결과를 기본 병렬 명령의 무결점 통과로 기록하지 않는다.

문서상 후속 정리: Legacy 무관 예시 표의 합계는 반올림 전 산식으로 82.25이며 최종 정수는 82다. 표의 82.4 표기와 구별하여 테스트했다. 현재 점수 범위나 기존 정본 문서는 수정하지 않았다.
