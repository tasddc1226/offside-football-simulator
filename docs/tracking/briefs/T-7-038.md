# T-7-038: 경기 선택에서 다음 커리어까지 이어지는 반복 플레이

## 목표와 기준

사용자가 2026-09-21 승인한 순서대로 네 단계를 구현하고 검증한다. 첫 경기의 선택이 실제 결과를 바꾸고, 커리어 기사를 공유해 같은 조건에 도전하며, 은퇴 선수를 친선 경기에 활용하고, 기억을 가진 인물과 다시 만날 수 있어야 한다. 네 단계 전체가 완료 범위이며 일부 단계만으로 종결하지 않는다.

- 기준 main: `6a2e9423d179a2fe030fe0ee65943340e13a0d5f`.
- 작업 브랜치: `T-7-038-growth-loop`, 격리 워크트리 `growth-loop`.
- 현재 신규 커리어 기준: 룰셋 `3.1.0`, 콘텐츠 `0.10.0`. 신규 행동 변경은 새 버전으로 추가하며 첫 단계의 후보는 `3.2.0/0.11.0`이다. 실제 등록 전 충돌과 계약을 확인한다.
- 참조: 루트 `CLAUDE.md`, `docs/tracking/README.md`, 기존 경기·공유·라커룸·관계 구현 및 해당 개발 명세. 정본 우선순위는 `docs/development > phases > screens > content`다.
- 이번 범위: 구현, 단계별 검증, 로컬 커밋과 전체 체인 통과 후 draft PR. main 병합과 운영 배포는 포함하지 않는다.

## 순차 완료 조건

### 1. 실제 경기 선택과 피드백 — FR-GROW-1

- [x] 자연스러운 신규 커리어의 첫 경기에서 의미 있는 선택을 경험한다.
- [x] 선택이 실제 득점·도움·승패 등 해당 장면의 결과에 반영된다. 설명이나 평점만 바꾸는 구현으로 대체하지 않는다.
- [x] 선택 → 경기 결과 → 관계 반응을 저장된 사실로 연결해 보여 준다.
- [x] 스코어, 개인·팀 통계, 순위표, 경기 기록과 후속 참조가 서로 일치한다.

1단계 승인 설계: `3.2.0/0.11.0`의 opt-in 경기 장면은 현재 step에서 대상 경기의 기본 결과를 계산한 직후, 이후 경기를 진행하기 전에 멈춘다. 새 pending chapter의 우선순위로 일반 사건 슬롯이 이 멈춤을 삼키지 않게 하되 필수 부상·대표팀·계약 결정을 보존한다. step 종료 조건 처리는 결정을 마칠 때까지 미루고 같은 step을 정확히 한 번 재개한다. 같은 step에 두 번째 장면은 열지 않는다. 슈팅·도움·선방·차단 결과에는 변경 전후 스코어와 개인 통계 귀속 영수증을 남긴다. 이후 진행 전에 리그 원장·누적 기록·컵 진출과 최종 평점 의존 선수단 상태를 재계산한다. 과거 버전의 진행과 결과는 바꾸지 않는다.

### 2. 커리어 기사와 동일 조건 도전 — FR-GROW-2

- [ ] 실제 은퇴 기록에서 기사 제목과 핵심 사실을 만들고 공개 페이지로 공유할 수 있다. 없는 수상·활약을 만들어 내지 않는다.
- [ ] 기존 공개 공유 endpoint가 없음을 기준으로 필요한 API를 추가한다. 비공개 retirement core는 source state·log를 포함하므로 절대 그대로 공개하지 않고 허용 필드만 기사에 투영한다. 명시적 공개 동의, 취소와 접근 종료를 제공한다.
- [ ] 공유 링크의 도전은 최초 seed·룰셋·콘텐츠 버전·선수 설정·선택 빌드를 재현한다. 원본 커리어의 소유권·복구 코드·스냅샷·명령 로그는 전송하지 않는다.
- [ ] 다른 방문자가 링크에서 독립된 신규 커리어를 만들고 계속 플레이한다.
- [ ] 공개와 동일 조건 도전은 각각 명시적 UI 동작으로 실행한다. 검증 중 타인의 데이터를 자동 공개하지 않는다.

2단계 설계 주의: 기사의 허용 필드는 서버가 소유권과 archive의 스키마·해시 무결성을 확인한 저장 기록에서 파생한다. 기존 retirement 저장은 명령 재생 기반 부정행위 검증을 하지 않고 정상 sync도 `PENDING`이므로 이를 `VERIFIED` 또는 부정행위 방지 증거로 표현하지 않는다. 클라이언트가 보낸 임의 기사 성적을 신뢰하지 않는다. 도전은 최초 `simulationMode`, 전체 seed, 정확한 불변 버전 쌍과 최초 draft를 고정한다. 포지션은 경력 중 바뀐 최종 `primaryPosition` 대신 최초 `preferredPosition`을 사용한다. 이름은 참가자가 새로 정할 수 있으며 동등한 경기 조건이 재현 기준이다. 기존 `isAcceptedSeasonVersion`은 시즌 버전 쌍 또는 승인된 `svc_season_1` 버전만 허용하므로 공개 도전에 결합된 제한적 admission 경로를 마련하고 일반 시즌 검증을 우회하지 않는다.

### 3. 은퇴 선수 친선 경기 — FR-GROW-3

- [ ] 저장한 팀의 은퇴 선수로 실제 플레이 가능한 결정론적 친선 경기를 실행한다. 빈 자리는 기본 동료가 채워 첫 은퇴 선수부터 이용할 수 있다.
- [ ] 포지션·선발·조합 선택이 경기 계산에 의미 있게 반영된다.
- [ ] 경기 결과 영수증을 저장하고 새로고침 이후에도 읽을 수 있다. 원본 선수의 커리어 통계는 바꾸지 않는다.
- [ ] 친선 경기는 명시적 UI 동작으로 시작한다.
- [ ] 실시간 PvP를 포함하지 않는다. 편성 화면만 추가하거나 결과를 임의 표시하는 것으로 대체하지 않는다.

### 4. 인물 기억과 재회 — FR-GROW-4

- [ ] 이름과 정체성이 일관된 인물이 플레이어의 실제 선택을 기억한다.
- [ ] 선택의 후속 반응과 실제 이적·경기·경력에 근거한 재회가 자연 진행에서 발생한다.
- [ ] 인물·시점·과거 사실이 맞는 경우에만 문구를 노출한다. 무관한 무작위 대사나 허구의 수상으로 대체하지 않는다.
- [ ] 신규 콘텐츠는 추가 버전으로 제공하고 과거 저장본의 사건 의미를 유지한다.

3단계 구현 계약: `FRIENDLY_V1`은 커리어 룰셋과 독립된 불변 친선 정책이다. 저장 팀의 은퇴 선발 한 명 이상이 필요하며 빈 선발은 명시적으로 표시한 기본 동료로 채운다. 지정된 모든 선수는 소유한 은퇴 archive의 무결성·버전 결합을 확인한다. 현역 또는 은퇴하지 않은 보관 선수는 후보라도 거부한다. 입력에는 필요한 선수 이름·포지션·20개 능력치·archive hash만 복사하며 private archive core, source state, 명령 로그를 넣지 않는다. 포메이션별 포지션 적합도, 패스 조합, 전술이 점유와 득점 확률에 반영된다. 후반 시작 시 후보 순서대로 적합한 자리 최대 세 명을 교체하고 출전 시간 창을 기록한다. 24개의 핵심 공격 장면을 계산하며 실시간 전체 경기나 PvP는 아니다.

친선 저장 계약: 팀 읽기 시점의 완전한 revision을 고정한다. 읽기 전에 바뀐 revision은 거부하고 읽은 뒤 편집된 팀은 이미 고정한 경기에 영향을 주지 않는다. 명시적 시작의 요청 키는 프로필 단위이며 동일 프로필의 병렬 시작·응답 유실 재시도는 단일 INSERT의 unique guard로 같은 결과를 돌려준다. 프로필 병합은 역사 결과 소유권을 이동하되 이전 프로필의 요청 키 namespace를 이전하지 않는다. 결과는 팀·선수 삭제 이후에도 비공개 역사로 남고 독립된 `/friendlies`에서 읽는다. 프로필 삭제는 soft profile 표시와 별도로 친선 행을 모두 삭제한다. 경기 결과는 원본 커리어 통계·snapshot·archive를 수정하지 않는다.

## 공통 계약과 계측

- **DATA-GROW-1**: 과거 저장 커리어의 고정 버전·저장 필드·재현 결과를 보존한다. 기존 콘텐츠 팩과 룰셋을 수정하지 않는다.
- **TEST-GROW-1**: 자연 진행으로 재현하고 같은 입력의 hash·결과 일치를 확인한다. 경기 스코어·통계·리그 원장 정합성, 재시도·중복 실행, 복원, 공유 취소, 잘못된 도전 입력을 검증한다. 합성 상태만으로 자연 경로 통과를 주장하지 않는다.
- **SCR-GROW-1**: 변경 화면을 360px, 키보드, 모션 감소로 실제 확인한다. 기존 시각 토큰과 저작된 한국어 문장을 사용한다.
- 기존 개인정보 안전 분석 경로로 방문 → 생성 → 첫 경기 → 첫 시즌 → 은퇴 → 재생성 및 공유·도전·친선 경기 전환을 계측한다. `contracts/analytics.ts`의 stage 허용 목록(onboarding/player/contract/season)을 확장한다. 선수명 원문·복구 코드·쿠키·공유 본문을 분석 로그에 넣지 않는다.
- domain은 순수·결정론을 지키며 시간·난수는 입력으로 받는다. 기존 패키지 의존 방향과 저장 계약을 준수한다.

## 단계별 품질 게이트

각 단계는 구현 → 요구사항별 자체 검토 → 관련 회귀·자연 진행·화면 검증 → 검증 증거 기록 후 커밋 순서로 진행한다. 실패·미실행·기준선 결함을 통과로 기록하지 않는다. 다음 단계는 앞 단계의 행동 계약과 검증을 인계받는다.

최종 통합 명령: `pnpm lint`, `pnpm lint:deps`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @offside/web check:bundle`, 관련 E2E. Node `22.23.1`, pnpm `11.25.0`을 사용한다. 테스트는 실제 위험과 요구사항을 검증하며 구현을 그대로 복제하는 테스트는 추가하지 않는다. 서버 포트는 충돌을 확인해 배정하고 자신이 실행한 프로세스만 종료한다.

## 현재 상태

- 2026-09-21: 1~3단계 구현·검증 완료. 4단계 다음 진행. 전체 목표는 진행 중이며 main 병합·배포는 하지 않았다.

### 1단계 인수 증거 (2026-09-21)

- 독립 검토 `phase1_review`: 승인, 차단 사항 없음. 역사 버전 파일 변경·비결정론 도입 없음 확인.
- 정적 검사: 루트 lint 10개 task와 `lint:deps` 통과. leaf tsconfig의 `allowImportingTsExtensions` 정합 수정 후 typecheck 9개 task 통과.
- 빌드: 루트 build 2개 task 통과, 초기 bundle gzip 109.06KB / 300KB 예산 통과. 지연 로드 engine 청크 1.84MB 경고는 남아 있으며 초기 청크 예산 통과가 전체 다운로드 크기를 보증하지 않는다.
- 패키지 검증: domain 995개, engine-client 110개 기준선 통과; content 504개, contracts 224개 통과. 최신 chapter·season 58개와 자연 경로 5개(GK·CB·CM·ST 및 역사 버전)에서 replay·schema·컨디션 1회 처리·변조 거부·멱등성 검증 통과. 버전 9개·API 시즌 호환성 4개·release guard 10개 통과.
- 장기 진행: root가 `--verify`로 4개 커리어 × 12시즌을 실행해 모두 은퇴, 실패 0건. 로컬 증거 디렉터리 `/tmp/offside-growth-loop-phase1.vDYS4d`.
- 실제 브라우저: ego `53p2`, 360px에서 신규 `3.2.0/0.11.0` ST 생성 → 입단 테스트 → 계약 → 훈련 3회 → 첫 승부처(화성전 90분 0:1)까지 자연 진행. 패스 선택 실패로 스코어 0:1 유지, 도움 0, 감독 신뢰 -2 영수증 확인. 새로고침 후 동일 결과 유지 및 키보드로 다음 진행 확인. 폭 360px에서 scrollWidth 360, 같은 CDP 호출 내 reduced-motion true 확인. 화면 기록 `/tmp/offside-growth-loop-phase1-mobile.png`.
- 증거 범위: 브라우저에서 성공 득점은 관찰하지 않았다. 성공에 따른 실제 점수 변화·통계·원장 정합성은 domain 검증 근거다. 전체 네 단계의 최종 통합 검증은 후속 단계 완료 후 수행한다.

### 1단계 E2E 후속 검증

- 기존 main의 온보딩·진행·일정 selector를 현재 UI에 맞추고 `1.7.2/0.6.6` 역사 경로 assertion을 보존했다. 신규 `3.2.0/0.11.0` 자연 경기 영수증·새로고침 복원 검증을 추가했다.
- 전용 포트 5391의 최종 관련 E2E 묶음: 53.8초, 9개 통과·실제 API 전용 1개 명시적 skip. skip은 통과 증거가 아니다. 결과 경로 `/tmp/offside-e2e-migration-final-5391`.
- 영향 파일 lint, E2E TypeScript 검사와 `git diff --check` 통과. 루트의 변경 검토 승인 후 테스트 파일 5개와 이 기록만 별도 커밋한다. 이 기록은 개발 중인 2단계의 완료나 검증을 의미하지 않는다.

### 2단계 인수 증거

- 구현·안전 검토 및 브라우저 인수 승인. 검토 중 최초 simulation mode 보존, 동기화된 로컬 데이터 덮어쓰기 방지, 원자적 재시도 보호를 수정했다.
- API 관련 4개 테스트에서 소유권·동의·허용 필드 투영, 손상 archive·CREATE 누락 거부, 최초 CHAPTER 이후 FAST로 변경한 커리어의 초기 mode 보존, 초기 profile·attributes·RNG와 새 이름, 병렬 멱등성, 공개 취소·삭제 이후 기존 구버전 도전의 후속 동기화, 프로필 병합·삭제를 검증했다. 공개 archive 무결성 검사는 부정행위 검증을 의미하지 않는다.
- web 관련 3개 파일 13개 테스트에서 공개·재시도·은퇴 연결 검증 통과. 후속 main 경로 테스트 4개, web/E2E TypeScript 검사와 관련 lint 통과. 루트 lint 10/10, typecheck 9/9, `lint:deps` 통과.
- 루트 build 및 bundle 검사 통과: 초기 gzip 109.18KB / 300KB. `db:check` 생성 결과 스키마 drift는 없지만 미커밋 migration 때문에 종료 코드 1이므로 통과로 기록하지 않는다. migration 커밋 후 재실행해야 한다.
- 로컬 자연 QA fixture: 12시즌·211개 명령, `/tmp/offside-retired-qa-20260921-1220`에 권한 0600으로 export. 명령별 hash를 검증하는 export helper와 정규화된 임시 출력 경로를 사용했다. 일반 PUT 6회 모두 200/PENDING, 앱의 reconcile 함수로 import했다(원시 state 주입 아님).
- 실제 UI에서 명시적으로 기사를 공개했다. 익명 GET 200, 허용 필드만 응답, `no-store` 확인. UI 공개 취소 후 기존 기사 GET과 새 도전 생성 모두 404. 이미 만들어진 도전 커리어는 취소 이후에도 자연 경로 선택·sync revision 5, 고정 버전 `3.2.0/0.11.0`을 유지했다. 브라우저 플레이는 같은 프로필이며 별도 소유자 격리는 API 테스트 증거다.
- 변경 화면은 360px 가로 넘침 없음, 키보드와 reduced-motion true 확인. 루트가 보관한 로컬 화면 기록: `phase2-article-mobile-v2.png`, `input-mobile.png`.
- 독립 전체 테스트: `pnpm exec turbo run test --concurrency=1 --output-logs=errors-only`, exit 0, 10/10 task(6 cache hit), 3분 51.904초. Vitest 293개 파일·2,984개 테스트 및 SEO 4개, 총 2,988개 통과. 패키지별 domain 998, content 504, contracts 224, engine-client 110, platform 51, fixtures 45, ui 108, scripts 19, api 241, web 684 및 SEO 4. landmark 수정 포함. 최초 기본 병렬 실행은 부하 중 시간·성능 기준을 초과해 실패했으며 코드를 바꾸지 않은 순차 재실행에서 모두 통과했다. 기본 병렬 명령 통과로 해석하지 않는다.
- 모든 실행은 로컬 검증이다. 공개 서버 배포·운영 데이터 게시·anti-cheat 검증 완료를 주장하지 않는다. 커밋 후 migration 게이트 결과는 별도로 확인한다.

### 3단계 인수 증거

- 은퇴 선수 친선 경기의 domain·API·UI 구현 완료, 독립 검토 최종 승인. 4단계는 미착수다.
- 실제 브라우저에서 저장 팀 `1ab10bd3-ea18-49b7-bd56-a89b7f9463ab` revision 1의 은퇴 LW 한 명으로 명시적 킥오프를 실행했다. 영수증 `2990bdc6-3bad-4d1f-bfd3-26aa16f35871`, 결과 5:2를 확인했다. 새로고침과 경기 기록에서 결과가 유지됐다.
- 팀 이름을 `은퇴선수 원정대 QA`로 바꿔 revision 2로 저장한 뒤에도 기존 영수증의 당시 팀 이름과 5:2 점수가 보존됐다. 원본 커리어 `qa-retired-ebdb4134539e-FW-0`의 revision 211 및 hash `1734997c76facb6a40b3090e874cc2ac031ff76866b042629061121100241e13`도 그대로였다.
- 360px 가로 넘침 없음, reduced-motion·키보드 흐름 확인. 로컬 화면 기록 `/tmp/offside-growth-loop-phase3-result-mobile.png`.
- migration 0011은 전용 로컬 DB에만 적용했다. 원격 변경·운영 배포 증거는 아니다.
- 검토에서 문구·소유자 격리와 입력 복사·역사 영수증 보존을 확인했다. 임의 선수 ID와 기본 동료 ID 충돌은 입력 선수 18명 전체의 ID를 예약하는 결정론적 생성으로 수정했으며 일반 입력 결과를 보존하고 악의적 ID 회귀 검증을 추가했다. 과거 콘텐츠·룰셋·fixture 파일은 변경하지 않았다.
- 최종 독립 검증: domain 71개 파일·1,004개 테스트(친선 6개 포함) 통과, API 37개 파일·247개 테스트 `--maxWorkers=2 --silent`로 101.61초 통과, UI 친선 5개 통과. lint 10/10, 의존 방향, typecheck 9/9, build 2/2, `git diff --check` 통과. 초기 bundle gzip 111.51KB / 300KB.
- 앞선 전체 순차 package 실행은 기존 API 테스트 2개의 5초 timeout으로 실패했다(245개 통과·2개 실패). 코드·timeout 변경 없이 worker 수를 제한한 전체 API 재실행은 통과했다. 전체 package 묶음이 통과했다고 주장하지 않는다. route test 파일 제외 경고·콘텐츠 dynamic import 무효 경고·engine 청크 약 1.844MB/390KB gzip 경고는 남아 있다.
- 정식 친선 E2E spec을 추가한 것은 아니며 화면 검증은 위 실제 ego 브라우저 증거다. 커밋 후 migration drift 검사는 별도로 확인한다.
