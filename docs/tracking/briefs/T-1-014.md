# T-1-014: Phase 1 E2E 완성(TEST-E2E-007·008·009)·키보드 전용 주 여정·5분 세션 측정·허브 LCP·CLS 재측정·완료 조건 표

## 목표
Phase 1 완료 조건 11개를 하나씩 측정하고 근거를 남긴다. 새로고침·중복 클릭·응답 유실에서 데이터가 깨지지 않고, 온보딩을 건너뛴 사용자가 5분 안에 첫 계약에 닿고, 키보드만으로 같은 여정을 끝낸다는 것을 자동화로 보인다.

## 참조 명세
- 요구사항 ID: TEST-E2E-001·007·008·009, FR-SAV-001, FR-REC-001, FR-ONB-001, 08 "Phase E2E"·"세션 길이·결정 예산 테스트"·"접근성 체크리스트"·"출시 차단 기준", 11 "테스트", `docs/phases/phase-01-career-vertical-slice.md` "완료 조건", 06 "접근성", 13 시각 시스템
- 읽어야 할 문서(순서대로): `docs/tracking/phase-1-plan.md` **D-18, D-22**(이 작업의 결정 전부), `docs/phases/phase-01-career-vertical-slice.md`, `docs/development/08-testing-strategy.md`, `docs/development/11-time-model-and-pacing.md` "테스트", `apps/web/e2e/README.md`, 기존 스펙 전부(`hub`·`create`·`first-contract`·`sync`·`settings-data`·`recovery-api`·`a11y`), `docs/tracking/decision-log.md`의 T-0-013 측정 항목(LCP·CLS 기준선)
- 정본 우선순위: phase-1-plan.md D-22 > phase-01 완료 조건 > 08 > 11

## 범위
- 만질 경로: `apps/web/e2e/**`, `apps/web/playwright.config.ts`, `apps/web/package.json`(스크립트만), `docs/tracking/phase-1-completion.md`(신설, 이 작업의 산출물. 문서 예외), `apps/web/e2e/README.md`. 화면 코드(`apps/web/src/**`)는 **테스트용 `data-testid` 추가만** 허용. 버그를 찾으면 고치지 말고 PR 본문과 완료 조건 표에 적는다.
- 만들 것:
  1. **TEST-E2E-007** `e2e/resilience.spec.ts`(스텁 API): (a) SCR-002 입력 도중 새로고침 → draft 값 복원(`UPDATE_PLAYER_DRAFT`가 저장한 값), SCR-004 확정 뒤 새로고침 → SCR-007에 그대로; (b) 확정 버튼을 `Promise.all`로 두 번 클릭 → 커리어 revision이 정확히 1 증가(허브 카드 1장, `/__dev/`가 아닌 화면 값으로 확인: 대시보드 revision 표시 또는 `data-testid`), 선수·계약 복제 없음; (c) `PUT /careers/{id}`를 첫 요청만 `route.abort()`로 유실 → 배지 "저장 다시 시도 중" → 두 번째 요청이 같은 `Idempotency-Key`로 감 → "저장됨"; (d) 명령 응답 대기 중(COMMITTING) 뒤로 가기 → 경고 대화상자(06 내비게이션), 재진입 시 확정 결과가 보인다.
  2. **TEST-E2E-008** `e2e/recovery-conflict.spec.ts`(`E2E_WITH_API=1`, 실제 api): 컨텍스트 A에서 첫 계약까지 진행 → 복구 코드 발급 → 컨텍스트 B에서 복구 → 같은 커리어 카드 → B에서 한 단계 진행(서버 revision 상승) → A에서 한 단계 진행 → A에 충돌 대화상자 → "다른 기기 진행 가져오기" → A 화면이 B의 진행과 같음. 반대 선택("이 기기 진행 유지")은 A에 카드 2장. T-1-012의 `recovery-api.spec.ts`와 겹치는 앞부분은 헬퍼로 뽑아 공유한다.
  3. **TEST-E2E-009 + 세션 길이** `e2e/session-length.spec.ts`(스텁 API): 온보딩 "건너뛰기" → 선수 만들기 → 진로 → 입단 테스트 → 제안 → 계약(SCR-010 KICKOFF)까지. 측정 둘: (a) 실제 자동화 시간(`performance.now()` 차이, 5분 미만 assert), (b) 최소 조작 시간 = 지나간 화면 수×1.0초 + 선택(라디오·카드) 수×2.0초 + 텍스트 입력 수×4.0초 + 확정(CTA) 수×1.5초 + 애니메이션 대기(건너뛰기 누르면 0). 화면·선택·입력·확정 횟수는 스크립트가 세어 `test-results/session-length.json`에 `{ automationMs, minimalHandlingSec, counts }`로 쓴다. 두 값 모두 완료 조건 표에.
  4. **키보드 전용 주 여정** `e2e/keyboard.spec.ts`: `first-contract`와 같은 여정을 `page.keyboard`(Tab·Shift+Tab·Enter·Space·화살표)만으로 끝낸다. 마우스·`click()` 금지(포커스 확인은 `toBeFocused`). 대화상자 포커스 트랩·복귀 포함.
  5. **접근성 확장** `e2e/a11y.spec.ts`: 모든 Phase 1 화면(SCR-034·001·002·003·004·007·008·013·014·009·010·029·030·법적 2개)을 axe로 검사(serious·critical 0). 추가로 (a) 텍스트 크기 150% 설정 + 360px에서 가로 스크롤 없음(`document.documentElement.scrollWidth <= 360`), (b) 모션 감소 설정에서 입단 테스트 결과가 즉시 표시(카운트업 없음: 값이 첫 프레임에 최종값), (c) `aria-live` 영역이 결과 확정 시 한 번만 갱신(`MutationObserver`로 개수 셈).
  6. **허브 LCP·폰트 CLS 재측정** `e2e/perf.spec.ts`(`vite preview` 빌드 대상. `playwright.config.ts`에 `E2E_PREVIEW=1`이면 `webServer`를 `pnpm build && vite preview --port 5175`로 바꾸는 분기): 커리어 카드 3장이 있는 허브(IndexedDB를 미리 채우는 헬퍼)에서 CDP `Network.emulateNetworkConditions`(4G: 다운 4Mbps, RTT 150ms)로 `largest-contentful-paint`·`layout-shift`(폰트 로드 전후) 값을 3회 측정해 중앙값을 기록. 목표 LCP 2.5초·CLS 0.1은 **assert하지 않고** 표에 적는다(T-0-013 기준선 2.5초 부근과 비교 문장).
  7. **완료 조건 표** `docs/tracking/phase-1-completion.md`: phase-01 "완료 조건" 11개 + ADR-008 "국외 이전 항목 명시"(법적 문서, T-1-012) 1개 = 12행. 열: 조건 / 측정값 / 통과 여부 / 근거(테스트 파일·테스트 이름 또는 PR 번호) / 비고. 통과하지 못한 행은 원인을 적고 고치지 않는다. Google 실계정 검증은 "U-003 대기"로 한 행 추가.
  8. **스크립트**: `e2e:api`(`E2E_WITH_API=1 playwright test`), `e2e:perf`(`E2E_PREVIEW=1 playwright test e2e/perf.spec.ts`). Turbo 파이프라인·CI는 건드리지 않는다(T-0-010).
- 만지지 말 것: `docs/`(7번 파일 제외), `packages/*`, `apps/api`, 화면 동작(`data-testid` 추가 외), 기존 스펙의 의미 변경(헬퍼 추출은 허용).

## 완료 조건
- [ ] `pnpm --filter @offside/web e2e` 통과, `pnpm --filter @offside/web e2e:api` 통과, `pnpm --filter @offside/web e2e:perf` 통과(각 스펙 수·시간을 PR 본문에). axe serious·critical 0건.
- [ ] `pnpm lint && pnpm lint:deps && pnpm typecheck && pnpm test && pnpm build` 전체 통과.
- [ ] `docs/tracking/phase-1-completion.md` 13행 전부 채움. 측정값은 숫자(초·ms·건수), 근거는 파일·테스트 이름.
- [ ] `session-length.json` 두 값과 LCP·CLS 중앙값을 PR 본문에.
- [ ] 발견한 버그 목록(재현 절차 포함)을 PR 본문 "범위 밖 발견 사항"에. 고치지 않는다.

## 제약
- 스펙은 결정론적이어야 한다(`waitForTimeout` 금지, 상태 기반 대기만). 재시도 0으로 3회 연속 통과를 PR 본문에 적는다.
- 실 네트워크 접근 금지(스텁 또는 로컬 api만). 외부 CDN 금지.
- 이미지·trace 파일을 저장소에 넣지 않는다(`test-results/`는 `.gitignore`).
- 커밋 메시지는 `T-1-014: …`로 시작, 브랜치는 `T-1-014-…`.
- 서브에이전트 리뷰를 띄우지 않는다. Orca PR 게이트용 `/review:pr` 1회만 예외(`/simplify`·병렬 fork 금지).
- PR 직전 `git fetch origin && git merge origin/main`으로 최신 main을 합친다. `pnpm-lock.yaml` 충돌은 손으로 고치지 말고 `git checkout origin/main -- pnpm-lock.yaml && pnpm install --no-frozen-lockfile`로 재생성한 뒤 전체 체인을 다시 돌린다.

## 진행 보고
- 체크포인트마다 `orca worktree set --worktree active --comment "..."` 갱신
- 막히면 질문을 PR 본문 또는 터미널에 남기고 멈춘다. 명세를 고치지 않는다.
- 끝나면 PR을 열고 본문에 요구사항 ID, 테스트 방법, 측정값 표, 버그 목록, 범위 밖 발견 사항을 적는다.
