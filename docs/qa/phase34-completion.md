# Phase 3·4 통합 마감 기록

작성: 2026-09-05. 작업 ID: T-4-022. 기준 main: `f7673ba`. 브랜치: `T-4-022-phase34-completion`.

## 인계 범위

Claude 세션이 중단된 시점의 PR #78·#79·#82·#83·#84를 로컬에서 원래 커밋을 보존하여 통합했다. 여기에 미완료 T-4-012 감사 수정, T-4-005 화면 묶음, T-4-018 시장 사유 표시와 실제 RUMOUR 진입을 보강했다. 사용자 승인대로 로컬 검증 및 CI가 통과한 뒤 통합 PR을 main에 병합한다. Phase 5 PR #77·점수/성장 밸런스 재설계는 포함하지 않는다.

기존 작업 폴더·사용자 플레이 데이터·원격 DB는 삭제하지 않았다. 브라우저 QA는 별도 로컬 포트 5196/8796과 새 QA 커리어를 사용했다.

## 구현한 내용

| 범위 | 변경과 이유 |
|---|---|
| RNG 결정론 | 감독 교체를 독립 파생 스트림으로 분리. 시장 경쟁자 요약에서 careerId를 제거해 fork-by-replay의 동일 입력 규칙 유지 |
| 시즌 중 live 상태 | 경기 진행 중 갱신된 부상·복귀·능력·역할·경기 기록으로 대표팀 및 재계약 조건 평가. 이미 기록한 자동 사양을 같은 walk에서 반복하지 않음 |
| 계약 안전 경로 | EXPIRED 전부 거절은 실제 1년 안전 재계약. FIRST_CONTRACT 전부/마지막 제안 거절 차단. 빈 사전 협상 제안은 다음 진행으로 종료 |
| 재계약 시점 | `nextContract`에 다음 계약 보관 → 기존 계약으로 이번 시즌 약속·태그 판정 → 결산 후 교체. 같은 시즌 결산 시장 중복 개방 억제 |
| 임대 만료 FA | 유령 원소속 stint를 만들지 않음. 서명 때 소속 기록 생성·팬 이월 1회. 감독 이력에서 세대를 복원해 경질된 최초 감독 ID 재등장 방지 |
| 화면 | SCR-016/018/021/022/024/032를 공통 이벤트 라우트의 맥락 패널로 구현. 부상 진단·재활 대가, 슬럼프 최근 경기, 관계 5축, 윤리·미디어 안내, 대표팀 안내 |
| 결과와 대시보드 | 관계·평판 단계적 공개, 감독·주장단·부상 잔여 표시. 결과는 당시 Snapshot의 실제 적용량을 우선하며 과거 결과에 최신 부상 상태를 섞지 않음 |
| 진입·팩 버전 | 저장된 커리어 팩으로 이벤트/챕터/명령 조립. 미해결 이적·임대 결정을 건너뛰는 프리시즌 진입 방지. 제안 없는 이적 창에서 RUMOUR 실제 개방 |

기존 480px 모바일 프레임, 공통 버튼·선택지·모션·커밋 중 이탈 방지·재시도 구조를 재사용했다. Apple/Emil UI 스킬은 기존 공간·상호작용 일관성을 유지하는 데 적용했으며 별도 디자인 시스템이나 이미지 자산을 도입하지 않았다.

## 골든 변경 범위

기존 fixture 명령 로그나 신규 fixture는 추가하지 않았다. 기존 생성기로 재생한 결과를 기존 골든 구조에 투영해 갱신했다.

| 기존 골든 | 변경 |
|---|---|
| career-02 FAST/CHAPTER, 05, 06, 07, 08, 10, 11, 13 | 이번 RNG 파생·상태 전이 수정에 따른 최종 stateHash 갱신. 저장된 시즌 결과·revision·메인 RNG draws는 유지 |
| market-02, market-03 | careerId 없는 파생 시드에 따른 경쟁자 순위·OVR 차이 요약 갱신 |
| 나머지 골든 | 내용 변경 없음 |

domain과 공용 fixtures에 있는 복제본을 함께 갱신했다. 기존 결정론·중간 직렬화·fork·계약 불변식 검증을 다시 실행했다. 이 기록은 옛 hash를 새 엔진이 그대로 생성한다는 뜻이 아니다.

## 검증 기록

- lint, 패키지 의존성 검사, 콘텐츠 검증(3개 팩, 경고 0), build, 번들 예산, 명암비 검사 통과.
- domain: 55파일 758건, API: 27파일 181건, web: 47파일 430건, content: 22파일 398건 통과. scripts 4·fixtures 45·contracts 210·ui 82·engine-client 82·platform 48건 통과(총 2,238건). 최종 typecheck 및 CI는 통합 PR의 검증 결과를 따른다.
- 최종 전체 E2E: **98 통과·6 기존 조건부 생략**. 앞선 실행의 selector 충돌은 ‘타 구단 관심’의 의미상 단일 definition으로 보정했다. 해당 시즌 E2E도 별도로 3/3 통과. 테스트 retry·skip을 추가하지 않았다.
- E2E의 기존 생략 6건은 실 API/복구 경로 및 production preview 성능 측정 등 별도 환경 게이트다. 이번 기본 E2E 통과를 실계정 인증·실기기 출시 검증으로 해석하지 않는다.
- 초기 JavaScript 약 **100.04KB gzip**(예산 300KB), 가장 큰 폰트 42.89KB(예산 200KB). production 번들에 DEV seed/팩 오버라이드 키 없음.
- 테스트 동시 부하에 민감한 API/domain은 분리 실행했다. 로컬 Node 25의 전역 Web Storage 충돌을 피하기 위해 web 테스트에 `NODE_OPTIONS=--no-experimental-webstorage`를 사용했다. 제품 코드나 테스트 제한시간을 이 때문에 바꾸지 않았다.

희귀 콘텐츠 검증은 ‘500개 고정 표본에서 12종 모두 반드시 당첨’ 대신 **각 이벤트의 조건 충족 + 각 presentation의 실제 선택 + 3개 챕터의 실제 선택**으로 구분했다. INJ-004/MEDIA-004가 후보였으나 선택되지 않은 관측은 숨기지 않는다. 모든 희귀 분기를 UI에서 전수 플레이했다는 뜻은 아니다. 사용자 최소 검증 원칙(D-62)에 따라 추가 대규모 fixture·seed sweep을 만들지 않았다.

완료 조건별 구체적 증거와 한계는 [인수 검토표](phase34-acceptance.md)를 참조한다.

## 실제 로컬 화면 확인

실제 엔진 명령으로 생성·진행한 새 커리어에서 확인했다. 부상 재활 선택은 화면에서 확정하여 결과까지 확인했다. 나머지 맥락 화면은 동일한 앱 명령 조립 함수를 통한 seed replay로 진입해 360×780에서 캡처했다. Snapshot/pending을 임의 주입하지 않았다.

| 화면 | 팩·seed·진입 | 캡처 |
|---|---|---|
| 부상 재활 결과 | 0.2.0 / offside-seed-search-2 / 시즌 1 step 6에서 재활 선택 | [결과](phase34/injury-result.png) |
| 슬럼프 | 0.2.0 / offside-seed-search-17 / 시즌 1 step 5 | [화면](phase34/slump.png) |
| 라커룸 | 0.2.0 / offside-seed-search-70 / 시즌 1 step 8 | [화면](phase34/locker-room.png) |
| 윤리 | 0.2.0 / offside-seed-search-2 / 시즌 2 step 9 | [화면](phase34/ethics.png) |
| 미디어 | 0.2.0 / offside-seed-search-0 / 시즌 1 step 4 | [화면](phase34/media.png) |
| 이적 루머 | 0.2.0 / offside-seed-search-0 / 시즌 2 step 7 | [화면](phase34/rumour.png) |

UI와 엔진 분기·선택 결과 검증은 구분한다. 캡처에 보이는 동기화 배지는 로컬 QA 환경이며 원격 배포 성공 증거가 아니다.

대표팀 SCR-032는 구현·타입·라우팅 및 기존 대표팀 도메인 검증으로 확인했으나 이번 자연 플레이 표본에서는 직접 캡처하지 못했다. 첫 선택 반복의 기존 seed는 RNG 정정 이후 더 이상 해당 시점의 소집을 재현하지 않아 제거했고, 0.3.0의 3개 seed×최대 15시즌 제한 재생에서도 미도달했다. 이는 전체 커리어에서 소집이 불가능하다는 증거는 아니다. 일반 이벤트 `EVT-NAT-002`의 선택 횟수는 전용 차출 pending/SCR-032 도달 근거로 사용하지 않는다. 실제 차출 화면·데뷔의 사용자 플레이 검증은 출시 게이트에 남긴다.

## 남은 출시 게이트

1. 기본 팩 **0.1.0 유지(D-56)**. Phase 4 콘텐츠 0.2.0/0.3.0 기본 활성화는 LINE TEST 기준선과 콘텐츠 승인 후 별도 결정이다. 이번 main 병합만으로 모든 새 이벤트가 기존 0.1.0 커리어에 나타나지는 않는다.
2. PROTOTYPE 문구의 SHIPPABLE 승격·실제 사용자 플레이 테스트·이벤트 빈도/대표팀 도달 난이도 검토. 룰셋 수치를 이번 작업에서 임의 조정하지 않는다.
3. Node/workerd 전용 해시 probe 확장(T-4-017), 실기기 성능·실계정 복구 및 실제 플레이 시간 검증은 별도 출시 QA 항목. 자동화 수초를 FAST 4~6분·CHAPTER 8~12분 달성으로 간주하지 않는다.
4. Phase 5 PR #77과 승인받은 밸런스 재설계는 사용자 재개 요청 뒤 최신 main에서 이어간다.
